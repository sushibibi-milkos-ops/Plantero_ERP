# Veri Bütünlüğü Kuralları (veri-critic her turda doğrular — tolerans sıfır)

| # | Kural | SQL dosyası |
|---|---|---|
| I1 | Σ(stock_quants.qty × lot.unit_cost) [ürün tipi kırılımı] = 150 / 151.02 / 152 / 153 hesap bakiyeleri (VUK ve UFRS ayrı ayrı); 151.01 (WIP) I1 dışında | `checks/01_inventory_value.sql` |
| I2 | Her stock_moves: qty × unit_cost = value; quant bakiyesi = Σ giriş − Σ çıkış (lokasyon+lot); quant.qty ≥ 0; reserved ≤ qty | `checks/02_stock_ledger.sql` |
| I3 | Her değerli stok hareketinin VUK ve UFRS fişi var; her stok fişinin move'u var | `checks/03_stock_journal_link.sql` |
| I4 | Her yevmiye: Σdebit = Σcredit; kapalı döneme kayıt yok; VUK fişi ↔ UFRS ikizi. **Tur 11, YENİ alt kural (regresyon koruması)**: eski sorgu yalnızca `twin_entry_id` DOLU iken tutarsızlığı yakalıyordu (`twin.twin_entry_id ≠ je.id` veya aynı `ledger`) — `twin_entry_id` tamamen NULL bırakılmış (tek deftere düşüp diğerine hiç düşmemiş) bir posted/reversed fişi hiç yakalamıyordu. Fresh seed'de bugün 0 ihlal (301 VUK + 301 UFRS fişin tamamı eşleşiyor, `postJournalEntry` çift kayıt disiplinini koruyor) — bu yalnızca gelecekte bir servis tek deftere yazıp diğerini atlarsa anında kırmızıya düşecek bir güvenlik ağı. **Aşama-2 muhasebe modülü, Tur 2 — P0, CANLI OLARAK DOĞRULANDI (twin_missing alt kuralı artık teorik değil, gerçekten tetiklenebilir)**: `apps/web/src/modules/accounting/components/manual-journal-form.tsx` (`/muhasebe/yevmiye/yeni`, "Manuel fiş" düğmesi) `Defter` alanında kullanıcıya üç seçenek sunuyor — "VUK + UFRS (ikisi)", "Yalnızca VUK", "Yalnızca UFRS" — ve seçilen değer `createManualJournalEntryAction` (`apps/web/src/modules/accounting/actions.ts:288-295`) üzerinden hiçbir ek denetimden geçmeden doğrudan `postJournalEntry({ ledger: input.ledger, ... })`'a taşınıyor; `postJournalEntry` de `ledger==='both'` değilse `twinEntryId`'yi hiç set etmiyor (`packages/core/src/accounting/journal.ts` — `ledgers = input.ledger==='both' ? ['VUK','UFRS'] : [input.ledger]`, twin bağlama bloğu yalnızca `ids.VUK && ids.UFRS` doluyken çalışıyor). Bu formu yalnızca `accounting.post` izni koruyor ve bu izne sahip roller arasında tam olarak bu ekranın hedef kullanıcısı olan **"Muhasebe"** rolü de var (Sistem Yöneticisi ve Genel Müdür ile birlikte) — yani günlük iş akışında bir muhasebeci "Yalnızca VUK" veya "Yalnızca UFRS" seçip kaydet dediği an (formda bunu engelleyen/uyaran hiçbir doğrulama yok, `balanced` kontrolü yalnızca borç=alacak'ı kontrol ediyor) veritabanı anında I4 `journal_entry_twin_missing`'e düşüyor. **Canlı egzersizle kanıtlandı**: fresh seed sonrası `postJournalEntry(tx, {ledger:'VUK', journalCode:'GEN', origin:'manual', lines:[{accountCode:'770',debit:100},{accountCode:'100',credit:100}]}, ...)` doğrudan core katmanından çağrıldı → satır başarıyla yazıldı (`journal_entries.id=ac5365b5-...`, `status='posted'`, `twin_entry_id=NULL`) → `pnpm db:check` anında **I4: 1 ihlal** verdi (`entity=journal_entry_twin_missing`, diff=1.0000) → test satırı silindi, `db:check` yeniden 0'a döndü (bkz. bu turun raporu). CLAUDE.md kural 7'nin ("her yevmiye ... stok/satış/alış kayıtları iki deftere de düşer, fark yalnızca değerleme/politika kalemlerinde") ruhuna göre VUK/UFRS farkı bir fişin TAMAMEN tek deftere düşmesi değil, aynı işlemin İKİ deftere de düşüp yalnızca değerleme satırlarının farklılaşması anlamına gelir — dolayısıyla "Yalnızca VUK"/"Yalnızca UFRS" seçeneği kural 7'nin doğrudan ihlalidir, meşru bir kullanım senaryosu değildir. **Kök neden**: `manual-journal-form.tsx`'in ledger seçici bileşeni (3 seçenekli) + `createManualJournalEntryAction`'ın bu değeri doğrulamadan `postJournalEntry`'e iletmesi. **Düzeltme önerisi**: (a) formdan "Yalnızca VUK"/"Yalnızca UFRS" seçeneklerini kaldır, yalnızca `'both'` bırak (en basit ve CLAUDE.md kural 7 ile tam uyumlu); veya (b) gerçekten meşru bir tek-defter senaryosu varsa (ör. yalnızca UFRS'e özgü bir değerleme düzeltmesi), bunu I4'ün `twin_missing` kontrolünü atlatan ayrı bir `origin`/flag (ör. `origin='valuation_only'`) ile işaretleyip I4 sorgusunu bu flag'i açıkça istisna tutacak şekilde güncelle — sessizce hiçbir işaretleme olmadan tek deftere düşen fiş üretimi hiçbir durumda kabul edilmemeli. **Tur 4 (veri-critic), YENİDEN DOĞRULANDI — HÂLÂ DÜZELTİLMEDİ**: `manual-journal-form.tsx` hâlâ 3 seçenekli (`z.enum(['VUK','UFRS','both'])`), `createManualJournalEntryAction` hâlâ doğrulamasız `postJournalEntry`'e iletiyor. Canlı egzersiz tekrarlandı: `postJournalEntry(ledger:'VUK', origin:'manual', 770/100 100 TL)` doğrudan çağrıldı → `journal_entry_twin_missing` 1 ihlal verdi, test satırı silinip 0'a döndü. **Tur 5 (veri-critic), YENİDEN DOĞRULANDI — HÂLÂ DÜZELTİLMEDİ**: `manual-journal-form.tsx:94`'teki `FormSelect` hâlâ 3 seçenekli (`{value:'both',...},{value:'VUK',...},{value:'UFRS',...}`) ve `actions.ts:287-295`'teki `createManualJournalEntryAction` hâlâ `input.ledger`'ı doğrulamadan `postJournalEntry`'e geçiriyor. Canlı egzersiz tekrarlandı (fresh seed sonrası, `packages/core` içinden doğrudan): `postJournalEntry(tx,{ledger:'VUK',journalCode:'GEN',origin:'manual',lines:[{accountCode:'770',debit:100},{accountCode:'100',credit:100}]})` → yeni fiş `id=e0880aaf-a687-4198-b582-4ceb78d6bffd`, `twin_entry_id=NULL` yazıldı → `pnpm db:check` **I4: 1 ihlal** (`journal_entry_twin_missing`, diff=1.0000) verdi → test satırı (journal_lines + journal_entries + audit_log) silinip 0'a döndü. Beş turdur aynı kök nedenle açık; düzeltme önerisi değişmedi (a) formdan tekli defter seçeneklerini kaldır. | `checks/04_journal_balance.sql` |
| I5 | Lot geri: her mamul lotu → origin_work_order_id dolu → iş emrinin ≥1 tüketimi var → her tüketim lotu origin_receipt_id (veya origin WO) dolu | `checks/05_lot_backward.sql` |
| I6 | Lot ileri: her delivery_lines.lot_id dolu (lotlu ürün) ve lot ASLA `quarantine`/`rejected` iken sevk edilmemiş; Σ tüketim + Σ sevk + Σ fire + eldeki ≤ lot initial_qty (+ sayım fazlası). **Tur 1 (Aşama-3, veri-critic), KAPSAM DÜZELTMESİ**: eski sorgu `l.status <> 'released'` idi (yani `recalled`/`expired` de dahildi) — bkz. I16 satırındaki aynı canlı doğrulama; `recalled`/`expired` çıkarıldı, yalnızca `quarantine`/`rejected` (CLAUDE.md kural 2 ile birebir) kaldı. Recalled/expired lotun mevcut konumu I27, recall sonrası yeni hareket ise I40 ile kapsanıyor. | `checks/06_lot_forward.sql` |
| I7 | Belge zinciri: her delivery→sales_order, invoice(sales)→delivery|sales_order, payment_allocation→invoice bağı document_links'te var; origin='manual' olmayan belge zincirsiz olamaz | `checks/07_document_chain.sql` |
| I8 | Miktar zinciri: sales_order_lines.delivered_qty = Σ delivery_lines.picked_qty; invoiced_qty ≤ delivered_qty; PO received_qty = Σ receipt_lines.qty | `checks/08_qty_chain.sql` |
| I9 | Cari bakiye: partners.balance = Σ satış faturaları (posted) − Σ tahsilat allocations (+iadeler) = 120.cari bakiyesi (VUK). **Bulgu (tur 8, P0 — kapsam boşluğu)**: `payments`/`payment_allocations` tabloları **0 satır** (`db:reset` sonrası da, 18 satış + 9 alış faturasının hepsinde `paid_amount=0`) — bu yüzden I9'un "tahsilat" terimi bugün her zaman sıfır katkı yapıyor ve kural yalnızca "bakiye = Σ fatura" alt kümesini doğrulamış oluyor; tahsilat çıkarma mantığı hiç egzersiz edilmiyor (bkz. altta ortak not) **Tur 7 düzeltmesi (P0, veri-critic — kontrolün KENDİ hatası, CANLI OLARAK YAKALANDI, İKİ AYRI SORUN)**: `allocated` CTE'si yalnızca `payment_allocations.amount_try` topluyordu (yalnızca faturaya TAHSİS EDİLMİŞ kısım). Ama gerçek uygulama (`packages/core/src/finance/payments.ts::recordPayment`, satır 28: "ana fiş zaten tüm tutarı (tahsis edilen + edilmeyen) 120/320'e işler") HER tahsilatın TAM tutarını 120'ye yazıyor ve `getPartnerBalance` doğrudan bu 120 bakiyesini okuyor — `payment_allocations`'a hiç bakmıyor. (1) Canlı doğrulama: arka planda oluşturulan tamamen tahsis edilmemiş bir avans tahsilatı (`PAY-2026-000019`, Trendyol Pazaryeri, 4.321,00 TL, `allocated_amount=0`) eski formülü hiç etkilemedi → I9 2 ihlal (diff=-4.321,00) verdi, halbuki `partners.balance` VE 120 ledger bakiyesi ZATEN doğruydu. Düzeltme: `allocated` artık `payments` tablosundan (`direction='inbound'`, `status='posted'`) DOĞRUDAN topluyor. (2) Bu düzeltme kendi başına YENİ bir kör nokta açtı: dövizli (ihracat) faturada `recordPayment` tahsis edilen kısmın kur farkını AYRI bir fişle (aynı ref_id) 120'ye ek `fx_difference` kadar dokunduruyor. Mevcut seed'deki BioGrün Handels GmbH (EUR ihracat, fatura kuru 37,20/tahsilat kuru 38,50, `PAY-2026-000010`, fx_difference=+351,00 lehte) bu yeni formülle anında 2 ihlal verdi (diff=-351,00) — gerçek bakiye (0,00) DOĞRUYDU, formül eksikti. Son düzeltme: `SUM(amount_try - fx_difference)`. Üçüncü kez `pnpm db:check` çalıştırıldığında 0 ihlal — hem avans hem kur farklı senaryo aynı formülle kapsanıyor. | `checks/09_partner_balance.sql` |
| I10 | Fatura: residual = grand_total − paid_amount; Σ allocations = paid_amount; status tutarlı. **Bulgu (tur 8)**: `paid_amount` her satırda 0 olduğundan (I9'daki not) bu kural şu an yalnızca `residual = grand_total` özel durumunu doğruluyor; "Σ allocations = paid_amount" dalı hiç veri görmüyor | `checks/10_invoice_residual.sql` |
| I11 | Banka: her bank_transaction en fazla 1 approved/auto_applied match; matched → payment/journal var; suggested/rejected match bakiyeyi etkilemez; eşleşen tutar = ödeme tutarı / fatura tahsis toplamı. **Bulgu (tur 8, P0)**: `bank_transactions` tablosu **0 satır** (3 banka hesabı var, `statement_balance` üçünde de 0) — I11 tamamen boş kümede GEÇTİ görünüyor, mutabakat/eşleştirme mantığı hiç çalıştırılmadı (bkz. altta ortak not) | `checks/11_bank_reconciliation.sql` |
| I12 | KDV: 391 = Σ satış fatura line_vat; 191 = Σ alış fatura line_vat (dönem bazında). **Tur 7 (veri-critic), P0, KIRMIZI → DÜZELTİLDİ (kontrolün KENDİ formül hatası, CANLI OLARAK KANITLANDI)**: eski formül yalnızca `kind='sales'`/`'purchase'` satırlarını sayıyordu, `kind='sales_return'`/`'purchase_return'` (iade faturası / credit note — `packages/core/src/accounting/invoices.ts::createCreditNote`, `/muhasebe/faturalar/[id]` "İade faturası kes" düğmesi, izin `accounting.post`) tamamen hariçti — ama `createCreditNote` gerçek 391/191 hesabına dokunuyor (ARCHITECTURE §7). `packages/db/src/seed/accounting-docs.ts`'in kendi dosya-başı yorumu bunu zaten itiraf ediyordu: "`createCreditNote` BİLEREK burada ÇAĞRILMAZ ... her iade faturası I12'yi kırar" — yani seed, bu canlı yolu BİLEREK hiç tetiklemeyerek db:check'i yeşil tutuyordu (I21'in Tur 4'teki "checks dosyası yanlış yazılmış, kimse düzeltmemiş" örüntüsünün burdaki hali). **Canlı doğrulama**: fresh seed'deki `INV-2026-000001` (vat_total=40,3960 TL) üzerinde `createCreditNote` rollback'li bir transaction içinde çağrıldı → AYNI transaction'da eski I12 SQL'i **1 ihlal** verdi (`vat_391_output`, period=2026-09, diff=-40,3960 — tam iade edilen KDV kadar) → SQL bu turda düzeltildi (`sales_vat`/`purchase_vat` artık `sales_return`/`purchase_return` satırlarını negatif işaretle dahil ediyor) → AYNI egzersiz (aynı rollback yöntemiyle) tekrarlandığında 0 ihlal. `pnpm db:reset && pnpm db:check` → 45/45 kural 0 ihlal (formül değişikliği fresh seed'i etkilemedi, seed zaten `createCreditNote`'u hiç çağırmıyor). **Kök neden dosyası**: `packages/db/src/checks/12_vat.sql` (servis kodu doğru, kontrol formülü eksikti) — düzeltme kalıcı, dosyanın kendi başlığında ayrıntılı. | `checks/12_vat.sql` |
| I13 | Kur farkı: dövizli fatura grand_total_try = grand_total × exchange_rate; tahsilatta fx_difference = amount × (tahsilat kuru − fatura kuru) ve 646/656 fişi var. **Bulgu (tur 8)**: fatura tarafı (a şıkkı) 1 EUR faturayla (`INV-2026-000012`) gerçekten test ediliyor ve GEÇTİ; ama b/c şıkları (tahsilatta kur farkı) `payments` boş olduğu için hiç egzersiz edilmiyor — ayrıca `fxDifference`/`fxJournalEntryId` alanlarını YAZAN tek bir kod satırı bile yok (bkz. altta ortak not), yani mantık şu an yalnızca şema düzeyinde var, servis düzeyinde yok | `checks/13_fx.sql` |
| I14 | Üretim: WO total_cost = material_cost + overhead_cost; Σ consumption.value = material_cost; Σ outputs.value = total_cost (kapalı WO); output lot unit_cost = value/qty | `checks/14_production_cost.sql` |
| I15 | 151.01 WIP bakiyesi = Σ (açık WO material_cost − Σ output value − Σ fire değeri) | `checks/15_wip.sql` |
| I16 | Karantina/red lotu müşteri veya üretim lokasyonuna hareket etmemiş. **Tur 1 (Aşama-3, veri-critic), KAPSAM DÜZELTMESİ (P1, kontrolün KENDİ tasarım hatası, CANLI OLARAK YAKALANDI)**: eski sorgu `l.status IN ('rejected','recalled','expired','quarantine')` ile TÜM geçmiş `stock_moves`(delivery/consumption)/`delivery_lines`/`work_order_consumptions` satırlarını CURRENT lot.status'e göre kontrol ediyordu. Canlı egzersiz: fresh seed'deki mevcut `RC-2026-000001` (status='simulation', rootLot='PL-260808-H1-12' — BADEM BAZI) üzerinde `packages/core/src/quality/recall.ts::initiate()` doğrudan çağrıldı (gerçek `/kalite/geri-cagirma` "Başlat" düğmesiyle birebir aynı kod yolu) → 4 lot (kök + 2. derece 3 hammadde lotu) `status='recalled'`e geçti; bu lotların TAMAMI recall öncesinde meşru şekilde `released` iken zaten sevk edilmiş/tüketilmişti (3 sevkiyat, 1 iş emri tüketimi) → `pnpm db:check` ANINDA **I16: 21 ihlal** (`stock_move_bad_lot_status` + `wo_consumption_bad_lot_status`) ve eş zamanlı **I6: 3 ihlal** (`delivery_line_lot_not_released`) verdi — hiçbirinde gerçek bir muhasebe/stok hatası YOKTU, üçü de recall'ün TASARIM GEREĞİ "zaten sevk edilmiş bir lotu hedefleme" amacının kontrolün "geçmiş asla bozulmamış olmalı" varsayımıyla çelişmesiydi. Kök neden: 'recalled'/'expired' giriş-zamanlı değil, lotun meşru `released` penceresi BİTTİKTEN SONRA geriye dönük atanan terminal durumlardır ('quarantine'/'rejected' ise tam tersine hiçbir zaman meşru "released" penceresi olmayan giriş-zamanlı durumlardır — bu ikisi tam kapsamda kalmalı). Düzeltme: I6b ve I16'nın üç alt kuralı da `recalled`/`expired`'ı listeden çıkarıp yalnızca `quarantine`/`rejected`'a daraltıldı; recalled/expired lotun MEVCUT fiziksel konumu I27 (zaten var, aynı egzersizde GERÇEK bir bug'ı doğru şekilde yakaladı — bkz. I27 satırı) ile, recall SONRASI oluşan YENİ (meşru olmayan) hareketler ise yeni eklenen I40 (zaman-duyarlı: `moved_at > lot.updated_at`) ile ayrıca kapsanıyor. Test verisi temizlenmedi — canlı egzersiz sonrası `pnpm db:reset` ile taze seed'e dönüldü. | `checks/16_lot_status_moves.sql` |
| I17 | Audit: son 24 saatteki her create/post işlemi için audit_log satırı var (server action'lar). **Tur 15, YENİ kapsam genişletme (P1, KIRMIZI, kök neden)**: kapsam listesine `bank_transactions`, `reconciliation_matches`, `channel_settlements` eklendi — daha önce bu üç tablo I17'nin `recent_records` listesinde hiç yoktu, yani bu tabloların audit kapsamı hiç ölçülmüyordu. Ölçüldüğünde: `channel_settlements` (2/2) tam kayıt-bazlı audit'e sahip (her satır `packages/core/src/sales/channelSettlements.ts` çağıranı tarafından `auditCreate(...)` ile ayrı ayrı yazılıyor) — bu ikisi 0 ihlal. **Aşama-2 birleştirme, YENİ bulgu + düzeltme**: `finance/payments.ts::recordPayment` kendi audit satırını yazmıyordu — mutabakat motorunun OTOMATİK uyguladığı (`persistAndApply` → `recordPayment`, hiçbir server action/seed katmanından geçmeyen) tahsilatlar `payments/<id>` için audit_log'da izsiz kalıyordu (canlı runner egzersizinde 1 ihlal olarak yakalandı); `recordPayment` artık `postJournalEntry` örüntüsüyle core katmanında kayıt-bazlı `writeAudit` yazar. Ama `bank_transactions` (8/8, **tamamı**) ve `reconciliation_matches` (12/13) kayıt-bazlı audit_log satırından tamamen yoksun — toplam **20 ihlal**. Kök neden: `packages/core/src/stock/ledger.ts::postStockMove` ve `packages/core/src/accounting/journal.ts::postJournalEntry` her çağrıda kendi audit satırını CORE katmanında yazar (`writeAudit` doğrudan bu dosyalarda çağrılıyor) — bu yüzden stock_moves/journal_entries seed'den bile %100 kapsanıyor. `packages/core/src/finance/bankReconciliation.ts::importStatement`/`runReconciliation` ise BİLİNÇLİ olarak bunu yapmıyor (dosyanın kendi tasarım notu: "audit yalnızca çağıran katmanda üretilir", bkz. `packages/db/src/seed/finance-payments.ts` ~166. satır yorumu) — hem `apps/web/src/modules/finance/actions.ts::runReconciliationAction` hem seed, bu toplu işlem için `record_id=NULL` ile TEK bir özet audit_log satırı yazıyor ("N hareket içe aktarıldı, M otomatik uygulandı" gibi), oluşturulan onlarca bireysel `bank_transactions`/`reconciliation_matches` satırının HİÇBİRİ kendi `record_id`'siyle iz bırakmıyor. Bu, CLAUDE.md kural 5'in ("Audit satırı: tablo, kayıt id, eylem...") açık ihlali ve aynı seed dosyasındaki `payments`/`channel_settlements`'ın (her ikisi de satır başına ayrı `auditCreate` çağrısıyla tam kayıt-bazlı) yanında tutarsız bir örüntü — tekil kullanıcı eylemleri (`ignoreTransactionAction`, `approveMatchAction`, `manualMatchAction`) doğru şekilde kayıt-bazlı audit yazıyor, yalnızca TOPLU üretim yolu (`importStatement` + `runReconciliation`'ın suggested/auto_applied ürettiği satırlar) kapsam dışı kalıyor. **Düzeltme önerisi**: `writeAudit`'i `postStockMove`/`postJournalEntry` örüntüsünde `bankReconciliation.ts`'in CORE katmanına taşı — `importStatement` içe aktardığı her `bank_transactions` satırı için, `runReconciliation`/`approveMatch` oluşturduğu her `reconciliation_matches` satırı için kendi `writeAudit` çağrısını yapsın; çağıran katmandaki (`actions.ts`, seed) tekil özet satırı ek bağlam olarak kalabilir ama tek kanıt olmamalı. **Aşama-2 muhasebe modülü, Tur 2 — YENİ bulgu, P1, CANLI OLARAK DOĞRULANDI**: `packages/core/src/accounting/journal.ts::reverseJournalEntry` (`/muhasebe/yevmiye/[id]`'deki "Ters kayıt" düğmesi, `reverse-journal-button.tsx` → `reverseJournalEntryAction`, izin: `accounting.post`) her ters kayıt işleminde İKİ YENİ `journal_entries` satırı (VUK+UFRS ikiz) üretir ama fonksiyonun sonundaki `writeAudit` döngüsü yalnızca İPTAL EDİLEN orijinal fişler için (`recordId: t.id`, `action: 'cancel'`) çalışıyor — yeni oluşturulan `revId` (ters kayıt fişinin kendisi) için `writeAudit` HİÇ çağrılmıyor; bu, `postJournalEntry`'nin kendi `writeAudit` döngüsünden (her ledger için ayrı, `action:'post'`) yapısal olarak farklı ve eksik bir örüntü — `reverseJournalEntry` `postJournalEntry`'yi çağırmıyor, `journalEntries`/`journalLines`'a DOĞRUDAN insert yapıyor, dolayısıyla `postJournalEntry`'nin otomatik audit güvencesinden hiç yararlanmıyor. **Canlı egzersizle kanıtlandı**: fresh seed sonrası mevcut bir posted VUK/UFRS ikiz fiş çifti (`JE-2026-000001`/`JE-2026-000002`) üzerinde doğrudan `reverseJournalEntry` çağrıldı → 2 yeni `journal_entries` satırı oluştu (`702e17e4-...` VUK, `59cd19a0-...` UFRS, ikisi de `status='posted'`) → `pnpm db:check` anında **I17: 2 ihlal** verdi (`entity=audit_missing`, her iki yeni id için `expected=1, actual=0`) → test verisi (yeni fişler + satırları) silinip orijinal fişler `status='posted'`e geri alındı, `db:check` yeniden 0'a döndü. Kök neden: `reverseJournalEntry`'nin audit bloğu yalnızca döngüdeki `t` (orijinal, iptal edilen) fişi referans alıyor, aynı döngüde ürettiği `revId`'yi hiç yazmıyor. **Düzeltme önerisi**: aynı `for (const t of targets)` döngüsü içinde, `revId` için de ayrı bir `writeAudit` çağrısı ekle (`action:'post'`, `recordId: revId`, `summary: "${t.ledger} ters kayıt fişi oluşturuldu: ${docNo}"`) — `postJournalEntry`'nin ledger-başına-audit örüntüsünü birebir tekrarla; mevcut `cancel` audit'i (orijinal fiş için) KALDIRMA, ekle. **Tur 4 (veri-critic), YENİDEN DOĞRULANDI — HÂLÂ DÜZELTİLMEDİ**: kod hâlâ aynı (`reverseJournalEntry`'nin audit döngüsü hâlâ yalnızca `t.id` için `action:'cancel'` yazıyor, `revId` için hiç yazmıyor) — canlı egzersizle (bkz. I37) tekrar kanıtlandı, 2 yeni ihlal (`journal_entries/<revId-VUK>`, `journal_entries/<revId-UFRS>`) üretildi, test verisi temizlenip 0'a döndü. **Tur 5 (veri-critic), YENİDEN DOĞRULANDI — HÂLÂ DÜZELTİLMEDİ (`reverseJournalEntry` bacağı)**: `packages/core/src/accounting/journal.ts:311-317`'deki `writeAudit` döngüsü hâlâ yalnızca iptal edilen orijinal fişler (`recordId: t.id`, `action:'cancel'`) için yazıyor, döngünün ürettiği `revId`'ler için hiç çağrılmıyor. Canlı egzersiz (fresh seed, mevcut posted VUK/UFRS ikiz stok-kaynaklı fiş çifti `JE-2026-000081`/`df9c410d-...` üzerinde `reverseJournalEntry` doğrudan çağrıldı) → 2 yeni fiş oluştu (`bb0f7033-...` VUK, `be5641ff-...` UFRS) → `pnpm db:check` **I17: 2 ihlal** (`audit_missing`, her ikisi için `expected=1,actual=0`) verdi; aynı egzersiz aynı anda **I1: 2 ihlal** (150 hesabı VUK+UFRS, diff=-5250,0000) ve **I37: 2 ihlal** de üretti (bkz. I37 satırı) — test verisi (yeni fişler+satırları+audit) silinip orijinal fişler `status='posted'`e geri alındıktan sonra `db:reset` ile taze duruma dönüldü, 0 ihlale döndü. **Ayrıca doğrulandı (I17'nin kendi dosya içi yorumundaki "Tur 15" bulgusunun aksine, artık DÜZELTİLMİŞ)**: `bank_transactions` (11 satır / 12 audit_log satırı) ve `reconciliation_matches` (18 satır / 20 audit_log satırı) artık kayıt-bazlı audit'e sahip — I29'un "tek kanonik mutabakat yolu" birleştirmesi bu boşluğu kapatmış; `17_audit_coverage.sql`'in başındaki "Tur 15" yorum bloğu artık GÜNCEL DEĞİL (kod düzeltildi ama dosya içi açıklama metni güncellenmedi) — belgesel borç, veri ihlali değil. | `checks/17_audit_coverage.sql` |
| I18 | Cari bakiye (tedarikçi tarafı, I9'un simetriği): Σ alış faturaları − Σ tahsis edilmiş ödemeler = −partners.balance (payable) = 320 hesabının (alacak−borç) bakiyesi (VUK). **Bulgu (tur 8)**: I9 ile aynı kök nedenden (`payments` 0 satır) "ödeme" terimi hiç egzersiz edilmiyor **Tur 7 düzeltmesi (I9 ile birebir aynı iki kök neden — bkz. checks/09_partner_balance.sql üst yorumu)**: `allocated` CTE'si `payment_allocations` yerine `payments` tablosundan doğrudan (`direction='outbound'`, `status='posted'`) TAM ödeme tutarını topluyor, ve dövizli ödemedeki ayrı kur farkı fişini de kapsamak için `SUM(amount_try - fx_difference)` kullanıyor. `pnpm db:check` sonrası 0 ihlal. | `checks/18_supplier_balance.sql` |
| I19 | Sipariş miktar tavanı: sales_order_lines.delivered_qty ≤ qty; purchase_order_lines.received_qty ≤ qty; purchase_order_lines.invoiced_qty ≤ received_qty | `checks/19_order_qty_ceiling.sql` |
| I20 | Kur kaynağı: dövizli satış faturasının exchange_rate'i fiş tarihine eşit/önceki en yakın exchange_rates.buying (TCMB) ile birebir eşleşmeli (I13 yalnızca fatura içi çarpımı doğrular, kaynağı doğrulamaz) | `checks/20_fx_rate_source.sql` |
| I21 | Devreden KDV (190) / Ödenecek KDV (360): vat_periods zinciri — bu ayın carried_from_prev = önceki ayın carried_to_next; **standart KDV mahsup mantığı** (payable = greatest(output_vat−input_vat−carried_from_prev, 0), carried_to_next = greatest(carried_from_prev+input_vat−output_vat, 0)); 190 defter bakiyesi = carried_to_next; 360 defter bakiyesi = payable. **Tur 4 (veri-critic), P0, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI (formül DÜZELTİLDİ — önceki turların "veri yokluğundan geçiyor" notu artık geçersiz)**: `packages/db/src/seed/accounting-docs.ts` artık `closeVatPeriod('2026-08',...)` çağırıyor ve `vat_periods` ilk kez gerçek veriyle doluyor (fresh seed: output_vat=442,5576 / input_vat=35.080,0000 / carried_from_prev=0 ⇒ net=+34.637,4424 — işletme net KDV ALACAKLISI, ödeyecek hiçbir şeyi yok). Ama `packages/core/src/accounting/vat.ts::closeVatPeriod` (kendi dosya-başı yorumunda AÇIKÇA itiraf ediyor: "standart KDV mahsup mantığının TERSİDİR... checks/21'in kendi formülünü birebir uygular, aksi halde db:check kırmızı kalır") `payable = max(net,0)` / `carried_to_next = max(−net,0)` atıyor — TAM TERS: gerçekte var olmayan 34.637,4424 TL'lik bir vergi YÜKÜMLÜLÜĞÜ `360`'a (ALACAK) yazılmış, gerçekte var olan aynı tutardaki devreden KDV ALACAĞI `190`'a hiç yazılmamış. **Kanıt (bağımsız muhasebe mantığıyla)**: yalnızca alış yapıp hiç satış yapmayan bir ayda (output_vat=0, input_vat=1000) işletme vergi dairesine ASLA borçlanamaz — eski formül bunu payable=1000 (360 ALACAK) yazardı, ki bu imkânsızdır. Canlı kayıtlar: `journal_entries` `42a0de9b-1d29-4cee-8f2a-94bc453e5006` (VUK) / `20b4b639-4bfd-4e0b-bac0-620b2520793d` (UFRS), ikisi de `360`'a 34.637,4424 TL yanlış alacak + `391.99`'a aynı tutar borç (mahsup), `190`'a hiç kayıt yok. Meğerse önceki bir tur `checks/21_vat_carryforward.sql`'in (b)/(c) formüllerini YANLIŞ yazmış (payable/carried_to_next etiketleri ters), sonraki core-katmanı ajanı da bu SQL'i (yanlışlıkla) "dondurulmuş/değiştirilemez" sanıp servisi ona uydurmuş — halbuki `packages/db/src/checks/*.sql` veri-critic'in kendi yazma alanı, şema gibi dondurulmuş DEĞİL. **Bu turda düzeltildi**: (b)/(c) formülleri doğru yöne çevrildi, (d) 190/(e) 360 defter-bakiyesi çapraz kontrolleri eklendi — artık gerçek seed verisiyle **2 ihlal** veriyor (`vat_period_payable_formula_mismatch` diff=34.637,4424, `vat_period_carry_to_next_formula_mismatch` diff=−34.637,4424); `packages/db/src/checks/checks.test.ts`'in "0 ihlal" beklentisi de bu yüzden bilinçli olarak KIRMIZI bırakıldı (I43'ün Tur 3'teki muamelesiyle aynı disiplin — kod düzeltilmeden testi yeşile zorlamak yanlış). **Kök neden dosyası**: `packages/core/src/accounting/vat.ts::closeVatPeriod`, satır ~103-104 (`payable`/`carriedToNext` ataması). **Düzeltme önerisi**: (1) iki `max(...)` atamasını takas et (`payable = max(net.neg(),ZERO)`, `carriedToNext = max(net,ZERO)`); (2) mevcut yanlış 2026-08 fişini (`42a0de9b.../20b4b639...`) ters çevirip `vat_periods` satırını silip dönemi yeniden kapat (idempotent guard `existing.journalEntryId` doluyken atladığı için önce temizlenmeli). | `checks/21_vat_carryforward.sql` |
| I22 | İş emri denormalize alan tutarlılığı: work_order_materials.consumed_qty = Σ work_order_consumptions.qty (materyal bazında); work_orders.scrap_qty = Σ work_order_scraps.qty; work_orders.yield_pct = round(produced_qty/planned_qty×100, 2) (planned_qty>0 ve dolu iken) | `checks/22_work_order_denorm.sql` |
| I23 | Satın alma faturalama zinciri: her değerli mal kabulünün (receipts, is_valued stok hareketi üreten) bağlı bir alış faturası (invoices.kind='purchase', receiptId) OLMALI — aksi halde 320.999 (Faturası Gelmemiş Alımlar) kapanmaz ve 191 (İndirilecek KDV) hiç doğmaz; purchase_order_lines.received_qty>0 iken invoiced_qty=0 olan satır aynı boşluğun sipariş tarafı yansıması. **Bulgu (tur 2→3 sabit, tur 6 doğrulandı)**: `packages/core/src/purchasing/invoicing.ts` içindeki `createPurchaseInvoiceFromReceipt` servisi yazılmış ve birim testli; artık `packages/db/src/seed/purchasing.ts` her `db:reset` sonrası tüm mal kabulleri bu servisle geriye dönük faturalıyor (tur 6'da 7/7, 9 mal kabulden 7'si değerli → 7 alış faturası) — I23 bu yüzden GEÇTİ görünüyor. **Ama bu yalnızca faturalama ucunu kapatan bir seed-workaround'u**: gerçek akışta (bir kullanıcı `/satin-alma` ekranından mal kabul girip fatura eşleştirse) hâlâ hiçbir yol yok — `apps/web/src/app/(app)/satin-alma` dizini hâlâ mevcut değil, `packages/core/src/purchasing/` içinde yalnızca `invoicing.ts` var (`orders.ts`, `replenishment.ts`, `whitelist.ts` — docs/modules/tedarik.md'nin şart koştuğu — hiç yazılmamış). Sipariş tarafının kendisi hâlâ tamamen eksik: bkz. I24 | `checks/23_purchase_invoice_gap.sql` |
| I24 | Satın alma sipariş zinciri: `receipts.purchase_order_id` NOT NULL ve `purchase_orders`'ta karşılığı var; `receipt_lines.purchase_order_line_id` doluysa `purchase_order_lines`'ta karşılığı var (draft/cancelled kabuller hariç). **Bulgu (tur 6, KIRMIZI, o zamana kadar geçerliydi)**: `purchase_orders` tablosu 0 satır, `reorder_rules` (kritik stok motoru) 0 satır — modül kodu hiç yoktu. **Tur 6 sonu**: `satin-alma` modülü (`packages/core/src/purchasing/{orders,replenishment,whitelist}.ts`, `apps/web/src/app/(app)/satin-alma/**`) yazıldı; `db:reset` sonrası `seed/purchasing.ts` (önce PO'lar, sonra `seedPurchasingBackfill` en sonda PO'suz kalan her mal kabulü geriye dönük bağlıyor) sayesinde **fresh seed üzerinde I24 GEÇTİ**. **Tur 7, YENİ bulgu — CANLI akışta hâlâ KIRMIZI**: `db:reset` sonrası, DB'yi eşzamanlı kullanan başka bir oturum gerçek `/depo/mal-kabul/yeni` formu üzerinden (Playwright ile, tarayıcı sürücüsü) 2 mal kabul oluşturdu (`GR-2026-000008`, `GR-2026-000009`, `origin='manual'`, `purchase_order_id=NULL`, ikisi de otomatik faturalanmış — I23/I25 bu yüzden hâlâ GEÇTİ) — I24 anında 2 ihlale düştü. Kök neden: `apps/web/src/modules/stock/components/receipt-form.tsx`'teki PO seçici kutusu kasıtlı olarak **"Sipariş seç (opsiyonel)"** — zorunlu değil; ve `packages/core/src/stock/receipts.ts`'teki `receiveGoods()` PO'suz kalan bir kabulü otomatik geriye dönük bağlamıyor (`createRetroactivePurchaseOrderForReceipt` yalnızca `orders.ts`'teki kendi yorumuna göre "seed/veri düzeltme senaryoları için" tasarlanmış, canlı yoldan hiç çağrılmıyor). Yani I23/I25'i kapatan "her mal kabulde otomatik fatura" güvenlik ağının I24 (PO zinciri) için bir eşleniği yok — seed'in `seedPurchasingBackfill` adımı tek seferlik, `db:reset`'ten sonra oluşan hiçbir canlı kabulü kapsamıyor. Düzeltme seçenekleri: (a) formda PO seçimini zorunlu kıl (sipariş dışı/numune kabul için ayrı, gerekçe zorunlu bir "PO'suz" yolu ekle ve I24'ü yalnızca o gerekçeli/manual işaretli kabuller için istisna tut — I7'nin `origin='manual'` istisna modeline benzer), veya (b) `receiveGoods()` içinde `purchaseOrderId` boşsa ve `partnerId` doluysa `createPurchaseInvoiceFromReceipt` ile aynı transaction'da otomatik olarak geriye dönük PO kurulmasını canlı yola da taşı. **Tur 8, DOĞRULANDI — DÜZELTİLDİ**: seçenek (a)+(b) birlikte uygulanmış — `receipt-form.tsx`'te `partnerId` artık şema düzeyinde zorunlu (`z.string().uuid('Tedarikçi seçin')`, "Sipariş seç" kutusu hâlâ opsiyonel ama tedarikçi zorunlu olduğundan PO'suz kalış artık kontrollü) ve `packages/core/src/stock/receipts.ts`'te kabul sonrası `purchaseOrderId` boşsa `partnerId` de yoksa hata fırlatılıyor, doluysa `createRetroactivePurchaseOrderForReceipt` aynı transaction'da otomatik çağrılıyor — canlı yol artık seed'deki güvenlik ağıyla eşdeğer. Fresh seed üzerinde I24 0 ihlal. | `checks/24_purchase_order_chain.sql` |
| I26 | Girdi kalite kontrolü (`qc_checks`) kararı ile lotun mevcut durumu birebir tutarlı: `status IN ('released','rejected')` iken bağlı hiçbir `qc_checks` kaydı `'pending'` kalamaz; kararı verilmiş kayıtların `disposition`'ı `stock_lots.status` ile eşleşmeli. **Bulgu (tur 8, yeni kural)**: tur 7 sonunda commit `b0ff4e5` ile `releaseLotAction`/`rejectLotAction` artık bağlı `pending` QC kaydını da aynı transaction'da karara bağlıyordu (önceki P2: QC kaydı süresiz `pending` asılı kalıyordu) — bu düzeltmeyi kalıcı regresyon koruması altına almak için eklendi. Fresh seed: 0 ihlal (1 lot hâlâ meşru şekilde `quarantine`+`pending`) | `checks/26_qc_lot_disposition.sql` |
| I27 | Lot durumu ile eldeki miktarı taşıdığı lokasyonun `usage`'ı uyumlu: `quarantine` lotu yalnızca `usage='quarantine'`, `rejected` lotu yalnızca `usage='rejected'`, `released` lotu asla karantina/red lokasyonunda değil, `recalled`/`expired` lotu `internal` (satılabilir) lokasyonda değil. **Bulgu (tur 8, yeni kural)**: `packages/core/src/stock/ledger.ts` → `enforceLotRules`/`enforceDirection` bunu üretim anında zaten engelliyor; I16'nın "hareket" odaklı kontrolüne ek olarak burada "anlık konum" odaklı ikinci bir savunma katmanı eklendi. Fresh seed: 0 ihlal. **Tur 1 (Aşama-3, veri-critic), P0, KIRMIZI, KÖK NEDEN, CANLI OLARAK KANITLANDI (bu kez GERÇEK bir servis hatası — I6/I16'daki gibi kontrolün kendi hatası DEĞİL)**: `packages/core/src/quality/recall.ts::initiate()` (satır 97-158) geri çağırmaya giren her lotu `tx.update(stockLots).set({status:'recalled', recallId, ...})` ile işaretliyor ve `recall_items`'a `action:'block', actionStatus:'done'` yazıyor — ama lotun ON-HAND miktarını fiziksel olarak HİÇBİR lokasyona TAŞIMIYOR (kodda tek bir `postStockMove` çağrısı yok; karşılaştırma: aynı dosyadaki `recordRecallAction('destroy', ...)` gerçekten `postStockMove(kind:'scrap', ...)` çağırıyor, ve `stock/expiry.ts::scrapExpired` da 'expired' için aynısını yapıyor — yalnızca `initiate()`'ın kendisi stok hareketi üretmiyor). Canlı egzersiz: fresh seed'deki `RC-2026-000001` üzerinde `initiate()` doğrudan çağrıldı (bkz. I16 satırındaki aynı olay) → kök lot `PL-260808-H1-12` (BADEM BAZI, 75 adet, `TIRE/MAMUL/R01`, usage='internal') dahil 4 lot `recalled` oldu, TÜMÜ hâlâ kendi orijinal `internal` (satılabilir) lokasyonlarında fiziksel olarak duruyordu → `pnpm db:check` ANINDA **I27: 4 ihlal** (`bad_lot_status_in_internal_location`, miktarlar 75/4,52/11,60/6,00 — toplam 97,12 birim, tam olarak `impact.qtyInStock` ile örtüşüyor) verdi. Bu, `recall_items.action='block'/actionStatus='done'`'ın YALANCI bir güvence ürettiği anlamına geliyor: ekranda "bloklandı" yazsa da ürün fiziksel olarak hâlâ satılabilir rafta, bir depo görevlisi FEFO/normal picking akışıyla bu stoğu farkında olmadan seçip sevk edebilir (ki bu durumda enforceLotRules gerçek zamanlı olarak engeller — ama stok fiilen orada durduğu ve "blocked" göründüğü sürece fiziksel karantina/izolasyon YOKTUR, yalnızca yazılımsal bir bayrak vardır). Kök neden dosyası: `packages/core/src/quality/recall.ts`, fonksiyon `initiate()`. **Düzeltme önerisi**: `initiate()` içinde, her etkilenen lotun mevcut `stock_quants` (qty>0) satırları için ilgili depodaki bir "recalled"/"quarantine" usage lokasyonuna (mevcut `getQuarantineLocation`/`getRejectedLocation` örüntüsünü izleyerek, gerektiğinde yeni bir `recalled` usage türü eklenerek) `postStockMove(kind:'transfer' veya yeni bir 'recall_block' kind, ...)` ile fiziksel transfer eklensin — tıpkı `scrapExpired`/`recordRecallAction('destroy')`'ın zaten yaptığı gibi; test verisi temizlenmedi, canlı egzersiz sonrası `pnpm db:reset` ile taze seed'e dönüldü. | `checks/27_lot_status_location_usage.sql` |
| I28 | Zorunlu denetim #10'un "reçete × üretim miktarı ± kayıtlı fire" bacağı: iş emri açılırken donan `work_order_materials.planned_qty`, `bom_lines` formülüyle (scale = `work_orders.planned_qty / boms.output_qty`; normal satır: `round(bom_lines.qty × scale × (1+scrap_pct/100), 4)`; yan ürün: fire çarpanı yok) birebir örtüşmeli — `packages/core/src/masterdata/boms.ts` → `explodeBom` tek üretim noktası. **Bulgu (tur 8, yeni kural — daha önce hiçbir I1-I25 kuralı bu formülü doğrudan doğrulamıyordu, yalnızca I22 gerçek tüketim/consumed_qty denormunu doğruluyordu**, plan tarafı denetimsizdi). Fresh seed: 0 ihlal | `checks/28_wo_material_bom_formula.sql` |

## Tur 8 ortak bulgu — "tahsilat/banka" modülü kod düzeyinde yok (I9/I10/I11/I13/I18'i etkiliyor)

`payments`, `payment_allocations`, `bank_transactions` tabloları fresh seed sonrası **0 satır**. Sebep, veri eksikliği değil — bu tabloları YAZAN hiçbir kod yok:

- `packages/core/src` içinde `payments`/`bank_transactions` tablosuna INSERT yapan tek bir servis dosyası yok (`grep -rn "insert(.*payments" packages/core/src apps/web/src` → 0 sonuç); `fxDifference`/`fxJournalEntryId` alanlarını okuyan/yazan tek satır kod yok.
- `apps/web/src/modules` altında `finans`/`tahsilat`/`banka` diye bir modül dizini yok (mevcut modüller: `kokpit, sales, auth, settings, production, masterdata, stock, purchasing`) — yani kullanıcı ekrandan "tahsilat kaydet" veya "banka ekstresi içe aktar" diyemiyor.
- `packages/db/src/seed/*.ts` içinde `payments`/`bank_transactions`'a hiçbir insert yok — seed bile bu tabloları doldurmuyor (I23/I24'te olduğu gibi bir "seed-workaround" bile devreye girmemiş).

**Sonuç**: I9, I10, I11, I13(b/c), I18 bugün GEÇTİ görünüyor ama bu, "tahsilat/ödeme/banka mutabakatı" muhasebe mantığının doğru çalıştığının KANITI DEĞİL — sadece bu dalların hiç çalıştırılmadığının göstergesi (I21/I23'teki "veri yokluğundan geçiyor" örüntüsünün finans tarafındaki eşleniği, ama kapsam çok daha geniş: CLAUDE.md'nin "belge zinciri: teklif → sipariş → irsaliye → fatura → **tahsilat**" akışının son halkası ve mandate'in 6/8/9 numaralı zorunlu denetimlerinin can damarı). **Kök neden**: `packages/core/src/finance/` (veya benzeri) dizini hiç açılmamış — `payments.ts` (tahsilat/ödeme kaydı + fatura tahsisi + kur farkı hesap/fiş üretimi) ve `bankReconciliation.ts` (ekstre içe aktarma + eşleştirme) yazılmamış; `apps/web/src/app/(app)/finans/**` ekranları yok. **Düzeltme önerisi**: satın-alma modülünün (I24, tur 6-7) izlediği yolu tekrarla — önce `packages/core/src/finance/{payments,bankReconciliation}.ts` + birim testleri, sonra `apps/web/src/modules/finance/{actions,queries,components}` + `/finans/tahsilat`, `/finans/banka` ekranları, sonra `packages/db/src/seed/finance-payments.ts` ile geçmişe dönük en az birkaç fatura tahsil edilmiş/eşleştirilmiş hale getirilsin (I23'ün seed-backfill örüntüsü) — ancak bu yalnızca I9-I18'i "geçer" hale getirmenin ötesinde, gerçek canlı akışın (I24 tur 7 örüntüsü gibi) da var olmasını gerektirir.
| I25 | GRNI tutarı: 320.999 (Faturası Gelmemiş Alımlar) bakiyesi (VUK ve UFRS ayrı ayrı) = Σ faturasız değerli mal kabul hareket değerleri. **Doğrulandı (tur 6, GEÇTİ)**: postStockMove'un 320.999 postalaması TL bazında doğru (I23/I24'ün "eksik" olduğu değil, "hiç tetiklenmediği" bir akış olduğunu kanıtlar) — tur 6'da 2 yeni faturasız mal kabul (canlı `/depo/mal-kabul/yeni` üzerinden, PO alanı olmadan oluşturuldu) 320.999'u 31.500,00 TL'ye açtı, tutar birebir tutarlı. **Tur 7 (veri-critic), P0, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI, HENÜZ DÜZELTİLMEDİ (servis katmanı — bu turda I12'nin aksine kontrolün DEĞİL, servisin hatası)**: `packages/core/src/accounting/invoices.ts::createCreditNote`'un alış (purchase_return) dalı (satır ~206-209) kaynağın HER satırının `accountCode`'unu (`byAccount` map) alıp aynen ALACAK yazıyor — ama `createPurchaseInvoiceFromReceipt` (`packages/core/src/purchasing/invoicing.ts:125`) ile üretilen bir alış faturasının satırlarının `accountCode`'u HER ZAMAN `'320.999'`. Yani böyle bir faturaya iade kesildiğinde `createCreditNote` 320.999'a tekrar ALACAK yazıyor — GRNI suspense hesabını, hiçbir faturasız mal kabulün karşılığı olmayan bir tutarla YENİDEN AÇIYOR (orijinal `createPurchaseInvoiceFromReceipt` bu hesabı BORÇ ile kapatmıştı; iade bunu simetrik ters çevirip yeniden alacaklandırıyor). Ama iade, mal kabulü "faturasız" durumuna GERİ DÖNDÜRMÜYOR — `invoices.status` değişmiyor, `receipts`/`purchase_order_lines` hiç dokunulmuyor, I23/I24 hâlâ 0 ihlal veriyor (fatura hâlâ "var ve iptal değil" göründüğü için) — yani 320.999'a giren bu yeni alacak bakiyesi HİÇBİR gerçek "faturası gelmemiş mal kabulü"ne karşılık gelmiyor, kalıcı ve gerçek dışı bir GRNI bakiyesi. **Canlı doğrulama (rollback'li transaction, kalıcı veri değişmedi)**: fresh seed'deki `PINV-2026-000001` (25.200,00 TL, `createPurchaseInvoiceFromReceipt` kaynaklı, satırı `accountCode='320.999'`) üzerinde `createCreditNote` çağrıldı → AYNI transaction içinde I25 SQL'i çalıştırıldığında **2 ihlal** verdi (`grni_320_999_balance`, hem VUK hem UFRS, `expected=0,0000` (gerçekte 0 faturasız mal kabulü var) `actual=25.200,0000` `diff=25.200,0000`) — I23 aynı anda 0 ihlal kaldı (kör nokta: I23 yalnızca "var/yok", I25 TL tutarını doğruluyor, tam da bunun için var). Ayrıca sevkiyat/stok tarafı da hiç dokunulmuyor: fiziksel olarak tedarikçiye iade edilen mal `postStockMove(kind:'return_out')` ile stoktan düşülmüyor (`return_out`/`return_in` stok hareket türleri şemada/`accounting/mapping.ts`'te tanımlı ama `grep -rn "kind: 'return_in'\|kind: 'return_out'" packages/core/src apps/web/src` → **hiçbir gerçek çağrı yeri yok**, yalnızca UI etiketleri/mapping mevcut — depo modülünün "kendi iade akışı" `invoices.ts:145`'in yorumunda vaat edildiği gibi yazılmamış) — bu da satış iadesinin simetriği (aşağıdaki not) ile aynı yapısal boşluk: 150-153 kalıcı olarak fazla gösteriyor/az gösteriyor, hiçbir invariant bunu yakalayamıyor çünkü hiçbir hareket hiç üretilmiyor. Test verisi kalıcı yazılmadı (rollback). **Kök neden dosyası**: `packages/core/src/accounting/invoices.ts::createCreditNote` (satır ~206-209, `byAccount` mantığı `accountCode==='320.999'` özel durumunu ayırmıyor). **Düzeltme önerisi**: (a) `byAccount` döngüsünde `accountCode==='320.999'` olan satırları GRNI'ye değil, kaynağın orijinal mal kabulünün stok hareket değerine karşılık gelen bir "alış iade değerleme" hesabına (ör. doğrudan ilgili 150/151/152/153 stok hesabına, ya da ayrı bir "iade beklenen" ara hesabına) yönlendir — 320.999'a ASLA dokunmasın; (b) VEYA iş kuralı olarak "receipt-tabanlı (320.999) bir alış faturasına iade kesilemez, önce depo tarafında fiziksel iade (`return_out`) işlenmeli" diyip `createCreditNote`'a bu durumda `DomainError` guard'ı ekle. Ayrıca (c) `packages/core/src/stock/*` içinde gerçek bir `return_in`/`return_out` `postStockMove` çağrı yeri (depo modülü, ör. `/depo/iadeler`) yazılmalı — hem satış hem alış iadesinin fiziksel stok tarafı bugün tamamen eksik (yalnızca finansal taraf var). Bu SQL (I25) hiçbir değişiklik gerektirmedi — kontrol zaten doğru formülle bu servis hatasını anında ve doğru şekilde yakalıyor; regresyon güvenlik ağı olarak aynen kalır. | `checks/25_grni_suspense_balance.sql` |
| I29 | Mutabakat kaydı bütünlüğü (I11'in tersi yönü): `reconciliation_matches.status IN ('approved','auto_applied')` ⇒ `payment_id` dolu VE bağlı `bank_transactions.status='matched'`. **Bulgu (tur 10, P0, kök neden)**: `apps/worker/src/jobs/reconciliationNightly.ts` (cron `0 2 * * *`, `apps/worker/src/queues.ts`'te kayıtlı) `packages/ai`'nin `matchBankTransaction`'ını kullanan AYRI bir eşleştirme yolu — yüksek güvenli sonuçlar için doğrudan `reconciliation_matches` satırı `status:'auto_applied'` ile ekliyor ama HİÇBİR ZAMAN `recordPayment` çağırmıyor (`payment_id` hep NULL) ve `bank_transactions.status`'u her zaman `'suggested'` bırakıyor (autoOk olsa bile) — bunun yerine tüketen kodu hiç olmayan bir `approvals` (kind='reconciliation') satırı ekliyor. Bu, tek gerçek yazma yolu olması gereken `packages/core/src/finance/bankReconciliation.ts::runReconciliation/approveMatch/manualMatch`'in (her zaman `recordPayment`'ı çağırıp `bt.status='matched'` yapan) yanında ikinci, tutarsız bir "yazıcı" — CLAUDE.md kural 1'in ruhuna aykırı (tek yazma noktası). Fresh seed 0 ihlal (worker hiç çalıştırılmadı, veri yalnızca `packages/core` akışından geldi) — bu kural worker canlıya alındığında/çalıştırıldığında oluşacak regresyona karşı kalıcı koruma. **Düzeltme**: `reconciliationNightly.ts`'i `packages/core/src/finance/bankReconciliation.ts::runReconciliation`'ı çağıracak şekilde yeniden yaz, ya da auto-apply davranışını kaldırıp yalnızca `suggested` üretsin. **Aşama-2 birleştirme (muhasebe+finans), DÜZELTİLDİ**: canlı mutabakat akışı TEK kanonik yola indirgendi — `@plantero/ai::runAiReconciliation` (orkestrasyon: `accounting/reconciliation.ts::buildCandidates` → `matchBankTransaction` → `persistAndApply`); worker `reconciliation-nightly`, `/muhasebe/mutabakat`, `/muhasebe/banka` ve `/finans/banka` hepsi bunu çağırır, onay/red/elle eşleşme de tür bazlı jenerik `approveReconciliationMatch`/`rejectReconciliationMatch`/`manualReconciliationMatch` üzerinden geçer (auto_applied/approved ⇒ aynı transaction'da `recordPayment` ya da gider/kredi fişi, `bt.status='matched'`). `finance/bankReconciliation.ts::runReconciliation` (fatura-only kural motoru) yalnızca `seed/finance-payments.ts` backfill'i ve kendi testleri için kaldı. Canlı egzersiz: fresh seed + `runAiReconciliation` (tutar birebir havale → auto_applied + tahsilat + fiş; masraf → suggested) sonrası I1-I35 0 ihlal. **Tur 7 düzeltmesi (P0, veri-critic — kontrolün KENDİ hatası, CANLI OLARAK YAKALANDI)**: eski (a) kolu TÜM approved/auto_applied satırlarda `payment_id` zorunlu tutuyordu. `packages/core/src/accounting/reconciliation.ts::applyMatch` `kind='loan_installment'` (ve 'expense'/'fee') için BİLİNÇLİ olarak `payment_id` ÜRETMEZ — `postLoanInstallmentPayment`/`postJournalEntry` ile yalnızca bir fiş atar ve `bank_transactions.journal_entry_id`'yi kendisi doldurur (payments tablosuna hiç dokunmaz). Canlı doğrulama: kredi L2 taksit #1 (`917947c5-...`, anapara 21.579,73 TL) banka mutabakatıyla (kind='loan_installment', status='approved') CANLI olarak ödendi işaretlendi (`ada857e1-...`, `packages/core/src/finance/loans.ts::postLoanInstallmentPayment` — bkz. I35 satırındaki not, aynı canlı olay) — eski (a) kolu anında 1 ihlal verdi, ama `bank_transactions.journal_entry_id` ZATEN doluydu ve status='matched' ZATEN doğruydu. Ayrıca eski (d) kolu `kind='marketplace_payout'`u desteklenmeyen sayıyordu — oysa `packages/core/src/sales/channelSettlements.ts` bu kind'ı `applyMatch`'ten TAMAMEN AYRI, kendi `recordPayment` çağrısıyla (payment_id ÜRETEREK) meşru şekilde kullanıyor; seed'in kendi ürettiği bir kanal hakediş ödemesi eşleşmesi bile eski (d) koluyla anında ihlal veriyordu. Düzeltme: (a) invoice/partner_on_account/marketplace_payout için payment_id zorunlu, (b) YENİ kol — loan_installment/expense/fee için `bank_transactions.journal_entry_id` zorunlu, (d) yalnızca gerçekten desteklenmeyen kind'ları (transfer/tax/unknown) yakalıyor. `pnpm db:check` sonrası 0 ihlal. | `checks/29_reconciliation_match_integrity.sql` |
| I30 | Banka hesabı / hareket / ödeme para birimi tutarlılığı: `payments.currency` = ilişkili `bank_accounts.currency`; `bank_transactions.currency` = ilişkili `bank_accounts.currency` (bir banka hesabı tek para biriminde tutulur; TL hesabına EUR yatırılamaz). **Bulgu (tur 10, P1, kök neden)**: `packages/core/src/finance/payments.ts::recordPayment` → `resolveCashAccount` yalnızca `bankAccounts.accountCode`'u okuyor, `input.currency` ile `bankAccounts.currency`'yi HİÇ karşılaştırmıyor; `bankReconciliation.ts::importStatement` de `line.currency ?? account.currency`'yi olduğu gibi kabul ediyor. `apps/web/src/modules/finance/components/record-payment-form.tsx`'teki banka hesabı seçici de seçilen `currency`'ye göre filtrelenmiyor — kullanıcı EUR tutarla TL hesabı seçebilir, form sessizce kaydeder (TL karşılığı yine doğru hesaplanır, muhasebe dengesi bozulmaz, ama 102.xx hesabına gerçekte var olmayan bir döviz hareketi işlenmiş olur; banka ekstresiyle mutabakat asla tutmaz). Fresh seed 0 ihlal. **Düzeltme**: `recordPayment`'a banka hesabı/para birimi eşleşme kontrolü ekle; formda `bankAccountOptions`'ı seçilen `currency`'ye göre filtrele; `importStatement`'a aynı kontrolü ekle. | `checks/30_bank_account_currency.sql` |

| I33 | Gelecek tarihli nakit olayı yasak: `payments.payment_date`, `bank_transactions.tx_date`, `journal_entries.entry_date` (status posted/reversed) `CURRENT_DATE`'i aşamaz — "bugün itibarıyla henüz gerçekleşmemiş" bir tahsilat/ödeme/banka hareketi/yevmiye zaten "posted"/"gerçekleşti" olarak kaydedilemez. **Bulgu (tur 13, P0, KIRMIZI, kök neden)**: `packages/db/src/seed/finance-payments.ts` satır ~56 `paymentDate: addDays(invoice.dueDate, -2)` — tahsilat tarihini faturanın VADE tarihine göre hesaplıyor, `CURRENT_DATE`'e göre değil. Standart 30 gün vadeli, bugün kesilmiş bir faturada (`invoice_date=bugün`, `due_date=bugün+30`) bu formül otomatik olarak bugünden ~28-30 gün İLERİ bir "tahsilat" tarihi üretiyor — seed'in kendi adı ("geçmişe dönük … dolgusu") bunun tam tersini vaat ediyor. Fresh seed (CURRENT_DATE=2026-09-04): 12 `payments` satırı (`PAY-2026-000004/005/006/009/010/011/012/013/014/017/018/019`) `payment_date` bugünden 9-46 gün ileride; bunlara bağlı 26 `journal_entries` satırı (VUK+UFRS ikizleriyle, hepsi `status='posted'`) aynı gelecek tarihle postalanmış; bağlı `invoices.status` zaten `'paid'`/`'partially_paid'` ve `paid_amount` tam güncellenmiş (ör. `PINV-2026-000002` 125.100,00 TL, `due_date=2026-10-04` olduğu halde bugün "ödendi" görünüyor) — yani sistem bugün, takvimde henüz gelmemiş bir tahsilatı/ödemeyi zaten gerçekleşmiş gibi gösteriyor; bu da I9/I18 cari bakiyesini ve nakit akışı ekranlarını "bugün itibarıyla" yanlış iyimser kılıyor (I9/I18 kendi içinde matematiksel olarak tutarlı kalıyor çünkü tarihten bağımsız Σfatura−Σödeme karşılaştırıyor, ama "bugünkü" tabloya gelecekteki nakdi şimdiden dahil ediyor). Ayrıca 5 `bank_transactions` satırı (`SEED-BT-005/006/007` + `INV-2026-000013`/`PINV-2026-000006` kaynaklı otomatik üretilenler) aynı kökten `tx_date` bugünden ileri, 2'si `status='matched'` (gerçekleşmemiş bir banka hareketi zaten "eşleşti" işaretli); bunu desteklemek için `exchange_rates` tablosuna da gerçek TCMB'nin asla yayımlamayacağı bir gelecek tarihli kur (`EUR 2026-09-15`) eklenmiş. **Düzeltme önerisi**: `finance-payments.ts`'teki tarih formülü `paymentDate = min(addDays(invoice.dueDate, -2), addDays(SEED_TODAY, -1))` gibi bugünü de alt sınır kabul edecek şekilde düzeltilmeli (yalnızca `due_date < bugün` olan faturalar "vadeden önce tahsil edilmiş" kurgusuna girmeli; `due_date ≥ bugün` olanlar ya hiç tahsil edilmemiş kalmalı ya da bugünün gerisinde ayrı bir tarih üretilmeli). Aynı disiplin canlı `packages/core/src/finance/payments.ts::recordPayment`'a da eklenmeli: `paymentDate > CURRENT_DATE` girişi reddedilmeli (VUK/UFRS'in "işlemin gerçekleştiği tarihte kayıt" ilkesi ve `postJournalEntry`'nin dönem kontrolü tarih sırasını değil yalnızca `fiscal_periods` aralığını doğruluyor — gelecek ay hâlâ "açık dönem" olduğu için bu kontrolü atlatıyor). | `checks/33_future_dated_cash_events.sql` |
| I32 | Kanal hakediş ÖDEME bütünlüğü (I31'in derinleştirilmesi, tur 12 — regresyon koruması): `status='paid'` her hakediş için bank_transaction'a bağlı TAM 1 onaylı/otomatik `kind='marketplace_payout'` `reconciliation_matches` kaydı VE dolu `payment_id` olmalı; o `payments.amount` = `net_payout`; `payment_allocations` toplamı = `net_payout` (on-account bırakılamaz); `payment→invoice` `document_links` kümesi `reconciliation_matches.invoice_ids` ile birebir aynı. **Gerekçe**: I31 (tur 11) yalnızca yüzeysel "bank_transaction_id dolu mu" diye bakıyordu — `markChannelSettlementPaid`'in ürettiği payment/allocation/document_links zincirinin herhangi bir halkasını (ör. netPayout'a kısmi tahsis, document_links atlanması) doğrulamıyordu. I32 mutasyon testleriyle doğrulandı (amount/allocation/document_links/match-status kırılımlarının her biri ayrı ayrı enjekte edilip yakalandığı transaction-rollback testleriyle teyit edildi). Fresh seed: 0 ihlal. | `checks/32_channel_settlement_payment_integrity.sql` |
| I34 | Kredi taksit takvimi iç tutarlılığı: `loan_installments.installment = interest + principal`; `remaining_after` zinciri (`seq=1` → `loans.remaining_principal − principal`, `seq>1` → önceki satırın `remaining_after − principal`); Σ`principal` (kredi bazında) = `loans.remaining_principal`; son taksitin (`seq = loans.remaining_installments`) `remaining_after`'ı = 0. **Bulgu (tur 14, P2, KIRMIZI, kök neden)**: `packages/db/src/import/nakitakisi.ts` satır ~217-234 — `taksit`, `faizBsmv`, `anapara` kaynak Excel'in "Kredi Takvimi" sayfasından ÜÇ AYRI hücre olarak bağımsız okunuyor ve her biri kendi başına `money4()` ile yuvarlanıyor; `taksit === faizBsmv + anapara` özdeşliği hiçbir yerde zorlanmıyor/yeniden hesaplanmıyor, kaynak dosyadaki kayan noktalı yuvarlama farkı olduğu gibi veritabanına taşınıyor. Fresh seed (`db:reset` sonrası): kredi **L1**'in 21 taksitlik takviminde 7 satır ihlal — `seq=9` (`installment=76218.7974` ≠ `interest+principal=76218.7973`, fark `0,0001` TL) ve bunun `remaining_after` zincirine yayılmasıyla `seq=8,9,11,14,15` satırlarında `±0,0001` TL sapma, ayrıca kredi toplamında (`Σprincipal` vs `loans.remaining_principal`) `-0,0001` TL fark; L2-L7'nin tamamı 0 ihlal (yalnızca L1'in kaynak Excel verisinde bu tekil yuvarlama sapması var). Zincir kendi kendini seq=21'de tam sıfıra kapatıyor (d şıkkı 0 ihlal) — yani hata kümülatif değil, tek bir ay içinde absorbe olmuş, ama CLAUDE.md kural 4 ("Para `numeric(18,4)`, `decimal.js` ile hesap; float yasak") ve mandate'in "1 kuruştan küçük fark bile KIRMIZI" ilkesi gereği bu yine de bir ihlaldir. **Düzeltme önerisi**: `nakitakisi.ts`'te satır bazında `principal = installment - interest` olarak TÜRETİLMELİ (üç bağımsız hücre yerine ikisi kaynak, biri hesaplanan), ya da içe aktarım sonrası bir doğrulama adımıyla `installment ≠ interest+principal` olan satırlarda `interest`'i `installment - principal`'a göre düzeltip fark kuruşunu son taksitte değil oluştuğu satırda kapatan bir mutabakat uygulanmalı. | `checks/34_loan_installment_integrity.sql` |
| I35 | Kredi hesap planı bakiyesi: aktif her `loans` kaydının `account_code`'una (300.01-300.07) karşılık gelen 300.xx alt hesabının posted `journal_lines` bakiyesi (Σcredit−Σdebit), VUK ve UFRS için ayrı ayrı, `loans.remaining_principal`'e eşit olmalı. **Bulgu (tur 14, P0, KIRMIZI, kök neden)**: `accounts` tablosunda 7 kredi için özel alt hesap zaten açılmış (`300.01`..`300.07`, isimleri `loans.product_name` ile birebir eşleşiyor — ör. "300.01 Banka Kredisi — Vakıfbank Tam Çıpa 1,5M") ve `Σ loans.remaining_principal` (aktif krediler) = **5.653.346,57 TL** gerçek bir banka kredisi yükümlülüğünü temsil ediyor, ama `packages/core/src` içinde `loans`/`loan_installments` okuyan/yazan **TEK SATIR KOD YOK** (`grep -rln "loans\b\|loanId" packages/core/src apps/web/src/modules` → 0 sonuç) ve `apps/web/src/app` altında kredi/`krediler` diye bir ekran da yok. Ne kredinin açılış bakiyesi (300.xx'e alacak) ne de tek bir taksit ödemesi (300.xx borç anapara + 780 borç faiz/BSMV + banka/kasa alacak) için hiçbir yevmiye fişi üretilmemiş — `journal_lines` içinde `account_code LIKE '300%'` olan **0 satır** var. Fresh seed: 7 kredi × 2 defter = 14 ihlal, her biri `actual=0,0000` (hiç kayıt yok) — bilanço bu 5,65 milyon TL'lik yükümlülüğü VUK'ta da UFRS'te de hiç göstermiyor. Bu, I9-I18'i döşeyen "tahsilat/banka kod yok" ortak bulgusunun (tur 8) ve I31'in "channel_settlements kod yok" bulgusunun (tur 11) kredi tarafındaki aynı örüntüsü — CLAUDE.md kural 1'in "her stok hareketi/muhasebe olayı `postJournalEntry` ile aynı transaction'da kaydedilir" ilkesi krediler için hiç uygulanmamış. **Düzeltme önerisi**: `packages/core/src/finance/loans.ts` yaz — (a) her aktif kredi için tek seferlik açılış fişi (300.xx'e alacak, VUK+UFRS ikiz), (b) `postLoanInstallmentPayment(loanId, seq)` → aynı transaction'da 300.xx borç (anapara) + 780 borç (faiz+BSMV) + banka/kasa alacak fişi üretsin ve `loan_installments.status='paid'` + `bankTransactionId`/`journalEntryId` alanlarını doldursun; ardından `packages/db/src/seed/finance.ts`'e I23/I31 örüntüsündeki gibi bu servisle üretilen bir geçmişe dönük açılış+ödeme dolgusu eklensin. **Tur 7 düzeltmesi (P0, veri-critic — kontrolün KENDİ tasarım çelişkisi, CANLI OLARAK YAKALANDI)**: `packages/core/src/finance/loans.ts` artık YAZILMIŞ durumda (bu satırdaki eski "hiç kod yok" bulgusu GÜNCEL DEĞİL) ve kendi üst yorumunda (satır 28-38) açıkça uyarıyor: `postLoanInstallmentPayment` BİLİNÇLİ olarak `loans.remaining_principal`'i DEĞİŞTİRMEZ, çünkü bu alan I34(c) gereği kredi takviminin TÜM satırlarının (durumdan bağımsız) anapara toplamına eşit SABİT bir referanstır (statik Excel içe aktarım değeri), canlı bir "kalan bakiye" alanı değildir — ve eski I35 tam olarak bu SABİT alanı "canlı ledger bakiyesi" ile eşit sayıyordu. Bu iki beklenti (I34-c: sabit toplam; eski I35: canlı bakiye = sabit toplam) yalnızca HİÇBİR taksit ödenmemişken birlikte doğru olabilir — yapısal olarak uyumsuzlar. Canlı doğrulama: kredi L2 (`6b66455d-...`) taksit #1 (anapara 21.579,73 TL) banka mutabakatıyla CANLI olarak ödendi işaretlenince (bkz. I29 satırındaki aynı olay) 300.02 hesabına gerçek bir borç kaydı düştü (1.500.000,00 → 1.478.420,27 TL), `remaining_principal` ise (tasarım gereği) 1.500.000,00'de sabit kaldı — eski I35 anında 2 ihlal verdi (VUK+UFRS, diff=-21.579,73), muhasebe/kredi modülünde HERHANGİ bir hata YOKTU. Düzeltme: "expected" artık `loans.remaining_principal` değil, o kredinin `status <> 'paid'` olan taksitlerinin anapara TOPLAMI (1.478.420,27 TL — ledgerla BİREBİR eşleşiyor). **schemaRequests**: `loans` tablosuna canlı bir `outstanding_principal` kolonu eklenmesi önerilir (bkz. checks/35_loan_ledger_gap.sql üst yorumu) — bugünkü çözüm doğru ama her okuma tüm takvimi tarıyor. `pnpm db:check` sonrası 0 ihlal. | `checks/35_loan_ledger_gap.sql` |
| I31 | Kanal hakediş mutabakatı: `channel_settlements.gross_sales`, o kanal+dönem için `sales_orders`'ta gerçekten var olan `grand_total` toplamını aşamaz; `status='paid'` iken `bank_transaction_id` dolu VE karşılığı `bank_transactions`'ta var olmalı; `paid_at` gelecekte olamaz (`> CURRENT_DATE`). **Bulgu (tur 11, P0, KIRMIZI, kök neden)**: `packages/db/src/seed/sales.ts::seedSettlements` (satır ~399-414) Trendyol/Hepsiburada için sabit demo rakamları (148.500 TL / 96.200 TL "brüt satış") elle yazıyor — aynı seed dosyasının birkaç fonksiyon yukarısında ürettiği GERÇEK `sales_orders`/`invoices` verisiyle (Trendyol tüm zamanlar toplam sipariş tutarı yalnızca ~29.195 TL) hiçbir ilişkisi yok; `channel_settlements`'tan `sales_orders`'a şemada FK bile yok. Ayrıca Trendyol kaydı `status='paid'`, `paid_at='2026-09-05'` (kayıt anındaki günden 2 gün İLERİDE — henüz gerçekleşmemiş bir ödeme "ödendi" olarak işaretli) ve `bank_transaction_id` NULL — `packages/core/src/finance/` içinde `channel_settlements` okuyan/yazan TEK satır kod yok (`grep -rn "channelSettlements" packages/core/src` → 0 sonuç), yani "kanal hakedişi ödendi" ekranda görünen tutarın bankada veya muhasebede hiçbir izi yok. **Düzeltme**: `channel_settlements`'ı gerçek `sales_orders`/`invoices` toplamından hesaplayan bir servis (`packages/core/src/sales/channelSettlements.ts` veya benzeri) yaz; ödeme anında `finance/payments.ts::recordPayment` ile aynı disiplinde `bank_transaction_id`/`payments` bağı kur; seed'i bu servisle üret (I23'ün `createPurchaseInvoiceFromReceipt` seed-entegrasyonu örüntüsünü tekrarla). | `checks/31_channel_settlement_reconciliation.sql` |
| I37 | Yevmiye ters kayıt koruması: stok/üretim kaynaklı bir yevmiye fişi (`ref_type IN ('stock_move','receipt','delivery','transfer','scrap','stock_count','work_order','quality_check')`) asla `status='reversed'` OLAMAZ — `reverseJournalEntry` yalnızca muhasebe tarafını ters çevirir, karşılık gelen stok hareketine/quant'a hiç dokunmaz. **Bulgu (bu tur, veri-critic Tur 4, P0, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI)**: `packages/core/src/accounting/journal.ts::reverseJournalEntry` hedef fişin `refType`'ini hiç kontrol etmiyor — herhangi bir posted fişi (kaynağı ne olursa olsun) koşulsuz ters çevirir; `apps/web/src/app/(app)/muhasebe/yevmiye/[id]/page.tsx`'teki "Ters kayıt" düğmesi (`ReverseJournalButton`, izin: `accounting.post`, "Muhasebe" rolü dahil) de yalnızca `entry.status==='posted'` şartına bakıyor, `refType`'e bakmıyor. Fresh seed sonrası `refType='stock_move'` olan posted bir VUK/UFRS ikiz fiş çifti (bir mal kabul hareketine ait, `SM-2026-000161`) doğrudan `reverseJournalEntry` ile ters kayıt edildi → `pnpm db:check` anında **I1 (150 hesabı, VUK+UFRS ikisinde -25.200,00 TL fark) VE I25 (320.999 GRNI bakiyesi, VUK+UFRS ikisinde -25.200,00 TL fark) KIRMIZIYA düştü** — I3 ise 0 ihlal vermeye devam etti: I3'ün `je.status IN ('posted','reversed')` koşulu "reversed"i de geçerli sayıyor, yalnızca fişin var olup olmadığını kontrol ediyor, stok tarafının da geri alınıp alınmadığını hiç sormuyor (I3'ün kör noktası). Aynı egzersizde I4 (`postJournalEntry(ledger:'VUK')` ile tek deftere yazım — bkz. I4 satırı) ve I17 (`reverseJournalEntry`'nin yeni ürettiği `revId` fişleri için `writeAudit` hiç çağrılmaması — bkz. I17 satırı) da aynı canlı egzersizde yeniden doğrulandı; her üçü de test verisi silinip orijinal durum geri yüklendikten sonra 0 ihlale döndü. **Düzeltme önerisi**: `reverseJournalEntry` başına guard ekle — hedef fişin (veya twin'inin) `refType`'i yukarıdaki fiziksel-stok kümesindeyse `DomainError` fırlatsın ("bu fiş bir stok hareketinden otomatik üretildi; düzeltmek için stok tarafındaki ilgili iade/iptal akışını kullanın"); `reverse-journal-button.tsx`'te de bu refType'lar için düğmeyi gizle/disable et (UI ikincil savunma, sunucu guard'ı birincil). **Tur 5 (veri-critic), YENİDEN DOĞRULANDI — HÂLÂ DÜZELTİLMEDİ, hem sunucu hem UI**: `packages/core/src/accounting/journal.ts::reverseJournalEntry` (satır 229-236) hâlâ `entry.status==='posted'` dışında hiçbir kontrol yapmıyor — `refType` hiç okunmuyor/karşılaştırılmıyor. `apps/web/src/modules/accounting/components/reverse-journal-button.tsx` hâlâ herhangi bir `refType` filtresi taşımıyor ve `apps/web/src/app/(app)/muhasebe/yevmiye/[id]/page.tsx:28`'deki render koşulu yalnızca `userCan(user,'accounting.post') && entry.status==='posted'` — yani `refType='stock_move'` olan bir fişin detay sayfasını açan herhangi bir "Muhasebe" rolü kullanıcısı "Ters kayıt" düğmesini görüyor ve tıklayabiliyor. Canlı egzersiz I17 notunda anlatılan aynı çağrıyla tekrarlandı: `refType='stock_move'`, posted, VUK/UFRS ikiz fiş (`069e9869-...`/`df9c410d-...`, `SM-2026-000161` mal kabul hareketine ait) üzerinde `reverseJournalEntry` **hatasız** tamamlandı (hiçbir `DomainError` fırlatılmadı) → `pnpm db:check` **I37: 2 ihlal** (`stock_linked_journal_reversed`, her iki orijinal fiş id'si için `actual=1` yani reversed) VE eş zamanlı **I1: 2 ihlal** (150 hesabı VUK+UFRS, -5.250,00 TL) verdi — stok tarafı (quant/lot) hiç değişmediği için muhasebe ile stok arasında gerçek bir sapma oluştu, tam olarak I37'nin öngördüğü senaryo. Test verisi (2 yeni reversal fişi + satırları + audit) silinip orijinal fişler `status='posted'`e geri alındı, ardından ortamdaki tutarsız artık audit_log satırları nedeniyle `pnpm db:reset` ile taze duruma dönüldü — reset sonrası 37/37 kural 0 ihlal. Beş turdur aynı kök nedenle açık; düzeltme önerisi değişmedi. | `checks/37_journal_reversal_guard.sql` |
| I36 | İhracat sevkiyat zinciri: `sales_orders.is_export=true` olan ve draft/cancelled olmayan her satış siparişi için `export_shipments.sales_order_id` ile karşılığı olmalı; `export_shipments.sales_order_id`/`invoice_id` doluysa `sales_orders`/`invoices`'ta yetim referans olmamalı. **Bulgu (bu tur, P1, KIRMIZI, kök neden)**: `docs/modules/ihracat.md`'nin (`/ihracat/sevkiyatlar`, `/ihracat/belgeler`, `/ihracat/kurlar`, proforma/packing-list/ETGB belge zinciri) şart koştuğu `packages/core/src/export/**`, `apps/web/src/modules/export/**`, `apps/web/src/app/(app)/ihracat/**`, `packages/db/src/seed/export.ts` hiçbiri yazılmamış — yalnızca `packages/db/src/schema/export.ts` (export_shipments/export_documents/export_packages tabloları tam şema ile var) ve `apps/web/src/lib/nav.ts`'teki "İhracat" bölümü (Sevkiyatlar/Belgeler/Kurlar, `export.view` izniyle) + `apps/web/src/lib/status.ts`'teki `export_shipment`/`proforma`/`packing_list` belge yönlendiricileri (`documentHref` → `/ihracat/*`) mevcut. Fresh seed sonrası `export_shipments`/`export_documents`/`export_packages` tabloları **0 satır**; halbuki `SO-2026-000023` (`is_export=true`, `status='invoiced'`, EUR, `INV-2026-000012` ile faturalanmış, tahsilatı `PAY-2026-000010` üzerinden kur farkı fişiyle (646/UFRS+VUK ikiz, 351,00 TL, doğru hesaplanmış — I13/I20 bu genel akışı zaten doğruluyor) tamamlanmış) hiçbir sevkiyat kaydına bağlı değil — mandate'in "İhracat" zorunlu denetiminin (#9) gerektirdiği sipariş→sevkiyat→proforma→packing list→belge takibi→gümrük/ETGB zinciri fiilen hiç kurulamıyor, yalnızca fatura-içi kur çarpımı test edilebiliyor. Bu, Tur 10'da muhasebe/finans modülü için tespit edilip sonradan düzeltilen "nav/status.ts var, route/core/seed yok" örüntüsünün ihracat modülündeki eşleniği — nav'daki 3 İhracat linki ve `documentHref`'in ürettiği export_shipment/proforma/packing_list bağlantılarının tamamı canlıda 404 verir. **Düzeltme önerisi**: I24 (tur 6-7, satın alma) ve I31 (tur 11, kanal hakediş) izlenen yolu tekrarla — önce `packages/core/src/export/{shipments,documents,etgb}.ts` (+`accounting/fx.ts` zaten mevcut) yaz, `createFromOrder` ile `is_export=true` bir siparişten sevkiyat/proforma/packing-list üret; sonra `apps/web/src/modules/export/**` + `apps/web/src/app/(app)/ihracat/{sevkiyatlar,belgeler,kurlar}` ekranları; sonra `packages/db/src/seed/export.ts` ile `docs/modules/ihracat.md`'nin tarif ettiği 3 sevkiyatlık (1 kapanmış ETGB + kur farkı fişli, 1 gümrükte, 1 taslak) geçmişe dönük dolgu — I23/I31 seed-entegrasyon örüntüsünü tekrarla. | `checks/36_export_shipment_gap.sql` |
| I38 | İş emri gerçekleşme kayıtları (`work_order_consumptions`/`work_order_outputs`/`work_order_scraps`) ↔ bağlı `stock_moves` birebir tutarlılığı (qty/value/unit_cost/lot_id/product_id/hareket türü) — her satırın `stock_move_id`'si dolu ve karşı tarafla tam eşleşmeli. **Bulgu (tur 7, kör nokta tespiti)**: şema yorumu "her satır bir stock_move üretir" der ama I14/I15/I22 yalnızca iş emri SEVİYESİNDE agrege tutarları (Σvalue, Σqty) kontrol ediyordu — satır bazlı FK'siz `stock_move_id` linkinin varlığı/doğruluğu hiç doğrulanmıyordu (toplamda birbirini götüren satır-seviyesi hatalara kördü). Fresh seed + canlı veri üzerinde 0 ihlal — yeni eklenen regresyon güvenlik ağı. | `checks/38_wo_move_linkage.sql` |
| I39 | Değerli stok hareketi tutarı = bağlı yevmiye fişi tutarı (VUK ve UFRS ayrı ayrı): her `stock_moves.is_valued=true` satırı için bağlı fişin `total_debit`=`total_credit`=`sm.value` olmalı. **Bulgu (tur 7, kör nokta tespiti)**: I3 yalnızca "fiş var mı/doğru move'a bağlı mı", I4 yalnızca "fiş içinde borç=alacak mı" sorusunu doğruluyordu — hiçbiri fişin TUTARININ `stock_moves.value` ile eşit olduğunu doğrulamıyordu; dengeli ama yanlış tutarlı (ör. yanlış maliyet taşıyıcısı) bir fiş ikisini de geçip parayı yanlış taşıyabilirdi. Fresh seed + canlı veri üzerinde 0 ihlal — yeni eklenen regresyon güvenlik ağı. | `checks/39_stock_move_journal_amount.sql` |
| I40 | Geri çağırma/SKT SONRASI zaman-duyarlı yeni hareket yasağı: lot.status ∈ (`recalled`,`expired`) iken, o statüye geçtiği andan (`stock_lots.updated_at`) SONRA `moved_at`'i olan hiçbir delivery/consumption `stock_moves` kaydı olamaz (I6/I16'nın 'quarantine'/'rejected' dışında kalan, terminal-statü-sonrası-yeni-hareket boşluğunu zaman damgasıyla kapatan tamamlayıcısı — bkz. I6/I16/I27 satırlarındaki Tur 1 notu). **Tur 1 (Aşama-3, veri-critic), YENİ kural**: I6/I16'nın 'recalled'/'expired'ı TÜM geçmişe uygulayan eski hali kaldırılınca (yanlış-pozitif ürettiği için, bkz. I16 notu), bu iki statü için "recall/SKT sonrası GERÇEKTEN yeni bir kaçak hareket olmasın" güvencesi boşta kaldı — enforceLotRules bunu üretim anında zaten engelliyor (`BAD_LOT_STATUSES`), I40 bunu veri düzeyinde ikinci kez, zaman damgasıyla doğrular. Fresh seed: 0 ihlal (henüz hiçbir lot bu statülere geçmediği için boş kümede geçer); canlı egzersizde (I16/I27 notundaki `RC-2026-000001` initiate() sonrası) da 0 ihlal — çünkü etkilenen 3 sevkiyat/1 tüketim hep recall'den ÖNCE (`moved_at < lot.updated_at`) gerçekleşmişti; bu kural yalnızca gelecekte `enforceLotRules` bypass edilirse (doğrudan SQL, regresyon) kırmızıya döner. | `checks/40_post_recall_movement.sql` |
| I41 | Tedarikçi kalite skoru (`supplier_scores.score`) formülü = qualityRate×50 + onTimeRate×30 + qtyAccuracyClamped×20 (satırın kendi denormalize alanlarından — `packages/core/src/quality/supplierScore.ts`); `partners.supplier_quality_score` = o tedarikçinin en güncel dönem skoru. **Tur 1 (Aşama-3, veri-critic), YENİ kural (kalite modülünde daha önce hiçbir I1-I40 kuralı bu formülü doğrulamıyordu)**: elle doğrulandı — fresh seed'deki 6 tedarikçinin tamamında (70,0000 / 69,0588 / 50,0000 / 100,0000 / 100,0000 / 85,0000) formül ve `partners.supplier_quality_score` birebir eşleşiyor. Fresh seed: 0 ihlal — saf regresyon güvenlik ağı (henüz canlı bir hata yakalamadı, ama daha önce hiç ölçülmeyen bir hesaplamayı artık her turda otomatik doğruluyor). | `checks/41_supplier_score_formula.sql` |
| I42 | Kritik stok motoru otomatik onay tutar sınırı: `is_auto_approved=true` bir satın alma siparişinde, `purchase_order_lines.reorder_rule_id`'nin işaret ettiği `reorder_rules.auto_order_max_amount` DOLU olan HER satır için `purchase_orders.grand_total` o satırın kendi sınırını aşamaz. **Tur 2 (Aşama-3, veri-critic), YENİ kural, P1, KIRMIZI (kod incelemesiyle doğrulandı — bugün seed'de tetiklenmiyor, saf regresyon güvenlik ağı)**: `apps/web/src/modules/purchasing/actions.ts::runReplenishmentAction` (satır ~252-257) bir tedarikçiye ait AI taslağındaki TÜM satırların `reorder_rules` kayıtlarından TEK bir birleşik `autoOrderMaxAmount` üretiyor — `amounts = rulesForOrder.map(r=>r.autoOrderMaxAmount).filter(a=>a!==null)`, `autoOrderMaxAmount = (amounts.length === rulesForOrder.length && amounts.length>0) ? min(amounts) : null`. Yani PO'daki satırlardan **BİR TANESİNİN** kuralı bile `autoOrderMaxAmount=NULL` ("bu ürün için sınırsız otomatik onay — yalnızca beyaz liste kontrol eder", `whitelist.ts` yorumu) taşırsa, uzunluk eşitliği koşulu (`amounts.length === rulesForOrder.length`) sağlanamadığından SONUÇ `null`'a düşüyor — yani PO'daki DİĞER satırların (ör. kendi sınırı 20.000 TL olan bir "Etiket" kuralı) sonlu tutar sınırı sessizce TAMAMEN iptal oluyor, kombine PO tutarı o sınırın kat kat üzerinde olsa bile `evaluateAutoOrderEligibility`'den `eligible=true` dönüp `isAutoApproved=true` ile onaysız gönderiliyor. **Canlı doğrulama (mutasyonsuz, paket-içi saf mantık kopyasıyla)**: `packages/core/src/purchasing/whitelist.ts`'teki `evaluateAutoOrderEligibility` + `actions.ts:252-257`'deki agregasyon birebir kopyalanıp gerçek DB'deki iki kural (tedarikçi `7bee4da7…`: "Etiket" cap=20.000 TL, "Kapak" cap=15.000 TL) üzerinde, yalnızca Kapak'ın cap'i hipotetik olarak NULL'a çekilerek çalıştırıldı → `computed autoOrderMaxAmount for COMBINED order: null` → `orderAmount=80.000 TL` (Etiket'in kendi 20.000 TL sınırının 4 katı) için `eligible: true` sonucu üretildi (script ve çıktı bu turun raporunda). Bugünkü seed'de bu kural 0 ihlal veriyor çünkü mevcut 3 whitelisted kuralın (`Saşe 10g`=12.000, `Etiket`=20.000, `Kapak`=15.000) ÜÇÜ de dolu bir `autoOrderMaxAmount` taşıyor — hiçbiri NULL değil (bkz. `reorder_rules` tablosu tam listesi) — bu yüzden agregasyon bugün her zaman `min(amounts)` dalına düşüyor ve doğru çalışıyor; bug yalnızca ileride biri "sınırsız" (`autoOrderMaxAmount=NULL`) olarak işaretlenip aynı tedarikçiye ait sonlu sınırlı bir kuralla AYNI AI taslağında birleştiğinde canlı olarak tetiklenir. **Düzeltme önerisi**: agregasyonu `amounts.length > 0 ? min(amounts) : null` şekline çevir (uzunluk eşitliği koşulunu kaldır) — yani sonlu sınırlardan biri bile varsa nihai sınır onların minimumu olsun, yalnızca TÜM satırlar gerçekten sınırsızsa (`amounts.length === 0`) nihai sınır da sınırsız kalsın. **Tur 3 (Aşama-3, veri-critic), DOĞRULANDI — DÜZELTİLMİŞ**: `apps/web/src/modules/purchasing/actions.ts:261-262` artık tam olarak önerilen şekilde (`const amounts = ...filter(a=>a!==null); const autoOrderMaxAmount = amounts.length > 0 ? amounts.reduce((a,b)=>a.lt(b)?a:b) : null;`) — uzunluk eşitliği koşulu kaldırılmış, kod satırı üzerinde doğrulandı. Kök neden kapalı. | `checks/42_reorder_auto_order_cap.sql` |
| I43 | Açık (henüz sevk edilmemiş) irsaliye satırı, engellenmiş durumdaki bir lota bağlı olamaz: `deliveries.status IN ('draft','reserved','picking','picked')` iken bağlı `delivery_lines.lot_id`'nin işaret ettiği `stock_lots.status` `quarantine`/`rejected`/`recalled`/`expired` olamaz. **Tur 3 (Aşama-3, veri-critic), YENİ kural, P1, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI**: I6'nın `delivery_line_lot_not_released` alt kuralı KASITLI olarak yalnızca 'quarantine'/'rejected'ı kapsar ve 'recalled'/'expired'ı TÜM `delivery_lines` geçmişinden hariç tutar (Tur 1'in I16/I6 kapsam düzeltmesi — recall'un "zaten sevk edilmiş" GEÇMİŞİ meşru şekilde hedeflediği için doğru bir tasarım), ama bu istisna yalnızca KAPANMIŞ (shipped/delivered/cancelled) irsaliyeler için doğrudur — I6/I16/I27/I40 hiçbiri HENÜZ SEVK EDİLMEMİŞ (draft/reserved/picking/picked) bir irsaliyenin recall/SKT SONRASI da bloklanmış bir lota bağlı kalıp kalmadığını sormuyordu. Canlı egzersiz: fresh seed'deki `RC-2026-000001` üzerinde `packages/core/src/quality/recall.ts::initiate()` doğrudan çağrıldı (I16/I27/I40'ın Tur 1 canlı egzersiziyle birebir aynı olay, bu kez Tur 2'nin `useReserved:true` düzeltmesinden SONRAKİ kod üzerinde) → kök lot `PL-260808-H1-12` `recalled` oldu ve fiziksel olarak karantinaya taşındı (I27 0 ihlal — Tur 1 düzeltmesi hâlâ doğru çalışıyor), quant.reserved_qty 15,00'ten 0'a doğru şekilde düştü (I2 0 ihlal — Tur 2'nin `useReserved:true` düzeltmesi de doğru), AMA bu rezervasyonu yaratan `DN-2026-000003` (status='reserved', henüz pick edilmemiş, dl.qty=15,00) irsaliye satırına `initiate()` hiç dokunmadı — recall sonrası da aynı (artık karantinadaki, recalled) lotu referans etmeye devam etti. `pnpm db:check` bu anlık görüntüde I43: 1 ihlal verdi (`open_delivery_line_blocked_lot`, id=`fa09ab77-930a-45dd-a726-12e7c6219714`); I1-I42 aynı anda 0 ihlaldi (bu satırdaki hiçbir mevcut kural bunu yakalamıyordu — saf kör nokta). Sonuç: bu irsaliye artık FİİLEN sevk edilemez (`enforceLotRules` recalled lotu yalnızca scrap/return_out/recall_return/count_loss dışına göndermeyi reddeder) ama ekranda/sorguda hâlâ "reserved" (aktif, ilerleyen bir teslimat) görünüyor — depo/satış ekibi fark etmeden bekletebilir, bağlı `sales_order_lines.delivered_qty` asla ilerlemez, müşteri siparişi sessizce askıda kalır. Test verisi temizlenmedi — canlı egzersiz sonrası `pnpm db:reset` ile taze seed'e dönüldü, I43 0 ihlale döndü. **Kök neden**: `packages/core/src/quality/recall.ts::initiate()` (muhtemel eşleniği: `packages/core/src/stock/expiry.ts::scrapExpired`, SKT tarafında aynı boşluğu taşıyabilir — bu turda ayrıca doğrulanmadı). **Düzeltme önerisi**: `initiate()` (ve `scrapExpired`) içinde, etkilenen her lot için status IN ('draft','reserved','picking','picked') olan `delivery_lines` satırlarını da bulup ya irsaliyeyi/satırı iptal edip `sales_order_line`'ı FEFO ile başka bir `released` lota yeniden planlamaya açacak şekilde ya da en azından `deliveries.status='cancelled'` + satış ekibine bir bildirim/onay kalemi üreterek ele al — sessizce askıda bırakma. | `checks/43_open_delivery_lot_status.sql` |
| I45 | Bütçe/nakit akışı "gerçekleşen" tazeliği: `budget_lines.actual`/`.variance` (hesap/kanal + dönem bazında `journal_lines`'tan hesaplanan) ve `cashflow_lines.actual_revenue`/`actual_collections`/`actual_fixed_expenses`/`actual_net_cashflow` (600/601/610/120/770/100/102 hesap bakiyeleri) HER ZAMAN muhasebedeki (posted/reversed) yevmiye satırlarıyla birebir uyuşmalı. **Tur 6 (veri-critic), YENİ kural, P1, KIRMIZI (canlı egzersizde), kök neden, CANLI OLARAK KANITLANDI**: `packages/core/src/finance/budget.ts::refreshActuals` (`/finans/butce`, "Yenile" düğmesi) bu dört alanı posted yevmiye satırlarından TEK SEFERLİK hesaplayıp yazan bir ÖNBELLEK — `postJournalEntry`/`postStockMove`'un HİÇBİRİ bunu tetiklemez (`grep -rn "refreshActuals" packages/core/src/accounting/journal.ts packages/core/src/stock/ledger.ts` → 0 sonuç) ve `apps/worker/src/jobs/cashflowRecompute.ts` (nightly `0 3 * * *`) da tetiklemez — o iş yalnızca mevcut `cashflow_lines.actual_net_cashflow`'u OKUYUP AI nakit tahmini üretir, hiç YENİLEMEZ. Canlı egzersiz: fresh seed sonrası `packages/core`'dan doğrudan `postJournalEntry({ledger:'both', origin:'manual', entryDate:'2026-09-05', lines:[{accountCode:'770.02',debit:5000},{accountCode:'100',credit:5000}]})` çağrıldı (2 yeni posted VUK/UFRS fiş) → `pnpm db:check`'in o anki 44/44 kuralının HİÇBİRİ bunu yakalamadı (44/44 GEÇTİ kaldı — hiçbiri budget_lines/cashflow_lines'a bakmıyordu), AMA `budget_lines` (period='2026-09', account_code='770.02') `actual=0,0000` göstermeye devam etti (gerçek muhasebe hareketi 5.000,00 TL, `diff=-5.000,0000`) ve aynı periyodun `cashflow_lines` satırı `actual_fixed_expenses=0,0000` (gerçek 5.000,00 TL) + `actual_net_cashflow` 5.000,00 TL YANLIŞ (iyimser) kaldı — yani `/finans/butce` ekranındaki "sapma" sütunu ve nightly AI nakit tahmini sessizce eski/yanlış veriye göre çalışır, hiçbir "son güncelleme"/tazelik uyarısı olmadan. Test verisi (2 journal_entries + journal_lines + audit_log) temizlenip `pnpm db:reset` ile taze seed'e dönüldü (reset sonrası I45 0 ihlal — seed'in son adımı `refreshActuals`'ı zaten çağırıyor). **Kök neden dosyası**: `packages/core/src/finance/budget.ts::refreshActuals` — hiçbir yazma yolundan otomatik tetiklenmiyor. **Düzeltme önerisi**: (a) `postJournalEntry` içinde etkilenen hesap kodu 600/601/610/770.*/120/100/102 önekleriyle eşleşiyorsa dokunulan `period`+`account_code`(veya `channel_id`) için nokta-atışı bir `budget_lines`/`cashflow_lines` güncellemesi tetikle; ve/veya (b) `/finans/butce`+`/finans/nakit-akisi` ekranlarına `cashflow_lines`/`budget_lines`'ın en son ne zaman hesaplandığını gösteren bir "Son güncelleme: …" rozeti ekle ve `cashflowRecompute` nightly işine (03:00) her koşuda ÖNCE `refreshActuals`'ı çağırma adımı ekle. | `checks/45_budget_cashflow_actual_freshness.sql` |
| I46 | Rezervasyon-sipariş bütünlüğü: iptal edilmiş (`cancelled`/`lost`) bir satış siparişinin, henüz sevk/iptal edilmemiş ('draft'/'reserved'/'picking'/'picked') aktif bir irsaliyesi kalamaz; böyle bir irsaliyenin FEFO ile rezerve ettiği (`delivery_lines.from_location_id` dolu, `qty−picked_qty>0`) fiziksel stok serbest bırakılmamış olamaz. **Tur 8 (veri-critic), YENİ kural, P0, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI**: `packages/core/src/sales/orders.ts::cancelOrder` yalnızca `salesOrderLines.deliveredQty>0 OR invoicedQty>0` durumunda iptali engelliyor — `stock/deliveries.ts::reserveFefo` ile zaten rezerve edilmiş ama henüz sevk edilmemiş ('reserved'/'picking'/'picked', deliveredQty hâlâ 0) bir irsaliyeyi hiç kontrol etmiyor ve iptal sırasında ona dokunmuyor. `deliveries.status` şeması (`packages/db/src/schema/stock.ts::deliveryStatusEnum`) bir `'cancelled'` değeri tanımlıyor ama `packages/core/src/stock/deliveries.ts` içinde bu duruma geçiren HİÇBİR fonksiyon yok (`cancelDelivery` yazılmamış) — yani rezervasyonu serbest bırakacak (`stock/ledger.ts::release`) hiçbir kod yolu mevcut değil. **Canlı doğrulama**: fresh seed'deki `SO-2026-000003` (status='confirmed', irsaliyesi `DN-2026-000003` status='reserved', 2 satırda toplam 15+8=23 birim `stock_quants.reserved_qty` ile FEFO rezerve — lot `c5d06fc6-...` 15 birim, lot `ceedd3f8-...` 8 birim) üzerinde `packages/core/src/sales/orders.ts::cancelOrder(tx, orderId, ctx, reason)` doğrudan çağrıldı → **hatasız tamamlandı**, sipariş `status='cancelled'` oldu → `pnpm db:check` (o anki 45/45 kuralın TAMAMI) 0 ihlal vermeye devam etti (I2 yalnızca `reserved≤qty`'yi kontrol ediyor — 15≤19 ve 8≤38 hâlâ doğru olduğundan geçiyor, rezervasyonun arkasında hâlâ canlı bir belge olup olmadığını hiç sormuyor) — `DN-2026-000003` `status='reserved'`de KALDI, `sales_order_id` hâlâ iptal edilen siparişi gösteriyor, `stock_quants.reserved_qty` HİÇ değişmedi: 23 birim fiziksel stok artık asla sevk edilmeyecek ölü bir siparişin arkasında süresiz kilitli — başka hiçbir sipariş bu stoğu FEFO ile göremez/kullanamaz, `getOnHand`/`pickFefo`'nun "kullanılabilir" hesaplaması (`qty−reserved`) kalıcı olarak düşük gösterir. Bu SQL yazıldıktan sonra aynı egzersiz **I46: 3 ihlal** verdi (1 `orphan_active_delivery_on_cancelled_order` + 2 `orphan_reserved_qty_on_cancelled_order`, diff=15,0000/8,0000); test verisi (sipariş durumu) `pnpm db:reset` ile temizlendi, reset sonrası I46 dahil 46/46 kural 0 ihlal. **Kök neden dosyası**: `packages/core/src/sales/orders.ts::cancelOrder` (irsaliye/rezervasyon kontrolü eksik). **Düzeltme önerisi**: `packages/core/src/stock/deliveries.ts`'e bir `cancelDelivery(tx, deliveryId, ctx)` eklenmeli — 'shipped'/'delivered' değilse tüm rezerve satırları için `stock/ledger.ts::release` çağırıp `delivery.status='cancelled'` yapsın; `cancelOrder`'ın `docType==='order'` dalında, mevcut `deliveredQty>0`/`invoicedQty>0` kontrolünün yanına siparişin aktif (henüz sevk/iptal edilmemiş) irsaliyelerini bulup aynı transaction içinde `cancelDelivery` ile kapatmalı (veya böyle bir irsaliye varsa iptali `ORDER_HAS_ACTIVITY` ile engellemeli — hangisi iş kuralına uygunsa, ama sessizce hiçbir şey yapmamak kabul edilemez). | `checks/46_orphan_reservation.sql` |
| I44 | İhracat sevkiyatı İPTAL sonrası belge zinciri temizliği: `export_shipments.status='cancelled'` iken `invoice_id` boş olmalı; `invoices.export_shipment_id` ve `sales_orders.export_shipment_id` iptal edilmiş bir sevkiyatı işaret edemez. **Tur 5 (veri-critic), YENİ kural, P1, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI**: `packages/core/src/export/shipments.ts::cancelShipment` (satır ~364-375) yalnızca `salesOrders.exportShipmentId`'yi temizliyor — kendi `invoiceId`/`deliveryId` alanlarını VE karşı taraftaki `invoices.exportShipmentId`'yi hiç temizlemiyor. `linkInvoice`'ın (aynı dosya) hiçbir `assertStatus` kısıtı yok, yani 'customs' dahil herhangi bir kapatılabilir (draft/proforma_sent/confirmed/packing/customs) durumdayken gerçek bir faturaya bağlanabilir; hemen ardından aynı sevkiyat `cancelShipment` ile iptal edilebilir. Canlı egzersiz (rollback'li transaction, kalıcı veri değişmedi): fresh seed'deki `EXP-2026-000001` (status='closed', invoiceId=`INV-2026-000012`) durumu geçici olarak 'customs'a çekilip `cancelShipment()` doğrudan çağrıldı → sonuç `status='cancelled'` AMA `invoiceId` hâlâ dolu (`038ef1d6-...`) VE `invoices.exportShipmentId` hâlâ aynı (artık cancelled) sevkiyatı gösteriyor (`1ed76025-...`) — hiçbiri temizlenmedi. Bu, mandate #5'in ("her belge zincirle kaynağına bağlı" — bir belgenin bağlı olduğu kaynak artık iptal/geçersizse bu bağ da geçersiz kılınmalı) doğrudan ihlali: ekranda/sorguda `INV-2026-000012` hâlâ "ihracat sevkiyat takibiyle bağlı" görünür, ama takip kaydı iptal edilmiştir — bir kullanıcı bu faturanın gümrük/ETGB durumunu sorguladığında iptal edilmiş bir sevkiyatın belgelerini gerçekmiş gibi görür. Fresh seed: 0 ihlal (henüz hiçbir sevkiyat 'cancelled' değil) — saf regresyon güvenlik ağı, canlı akışta (bir kullanıcı `/ihracat/sevkiyatlar/[id]`'den fatura bağlanmış bir sevkiyatı iptal ettiğinde) anında tetiklenir. **Düzeltme önerisi**: `cancelShipment` içinde `s.invoiceId` doluysa aynı transaction'da `tx.update(invoices).set({exportShipmentId:null})` çağır VE kendi `invoiceId`/`deliveryId` alanlarını da null'a çek (salesOrders.exportShipmentId için zaten uygulanan örüntüyü invoiceId/deliveryId'ye de tekrarla) — ya da iş kuralı "faturalanmış bir sevkiyat iptal edilemez" ise `assertStatus`'a `invoiceId IS NOT NULL` durumunda bir `DomainError` guard'ı eklensin. | `checks/44_export_shipment_cancel_orphan.sql` |
| I47 | Üretim kökenli lot miktar dengesi (docs/modules/kalite.md `/kalite/izlenebilirlik` vaadi: "giriş = tüketim + sevk + fire + eldeki"), TEK kanonik kaynaktan (`stock_moves`+`stock_quants`) — production kökenli lotlar için `Σ(production/byproduct/count_gain/return_in/recall_return) = Σ(consumption/delivery/count_loss/return_out) + Σ(scrap YALNIZCA kaynağı gerçek/stoklu lokasyon iken) + eldeki`; `recordScrap()`'ın virtual→virtual WIP fire hareketleri (kaynak usage='production', ledger.ts VIRTUAL_USAGES) bilerek dışlanır. **Tur 8 düzeltme turu (veri-critic), YENİ kural, P2, kök neden, CANLI OLARAK KANITLANDI**: I6'nın `lot_qty_exceeds_initial` alt kuralı (`checks/06_lot_forward.sql`) bu dengeyi `scraps`/`work_order_scraps` denormalize ara tablolarından okuyor; ama `packages/core/src/production/finish.ts::recordScrap()` `work_order_scraps.lot_id`'yi (şemada nullable kolon var) HİÇ yazmıyor. Canlı doğrulama: fresh seed'deki iki mamul lotu — `PL-260816-H1-01` (id `2d570b21-...`, WO fire 0,6000) ve `PL-260823-H2-01` (id `974f57d1-...`, WO fire 0,5000) — ikisinde de `work_order_scraps.lot_id IS NULL` iken karşılık gelen `stock_moves` satırı (`kind='scrap'`, `lot_id` DOLU, `from` lokasyonu usage=`production` — SANAL) doğru şekilde mevcut; I6'nın `ws` LEFT JOIN'i (`work_order_scraps.lot_id` üzerinden) bu iki lot için sessizce 0 döner ve I6 hiçbir zaman gerçek fiziksel dengeyi test etmeden 0 ihlale düşer. Regresyon riski: `work_order_scraps.lot_id` ileride doldurulmaya başlarsa I6'nın mevcut formülü bu kez WIP fireyi fiziksel çıkışmış gibi sayıp YANLIŞ POZİTİF üretir, çünkü o hareket hiçbir zaman `initial_qty`'ye girmedi (`apps/web/src/modules/quality/queries.ts::getTraceForLot`'un Tur 8'de düzelttiği hatanın birebir SQL karşılığı) — `pnpm db:check` bugünkü haliyle bu kör noktayı hiçbir şekilde yakalayamaz. Yeni `checks/47_production_lot_qty_balance.sql` `stock_moves`/`stock_quants`'tan DOĞRUDAN hesaplayarak bu kör noktayı SQL düzeyinde kapatıyor; fresh seed'de (yukarıdaki iki lot dahil) 0 ihlal — canlı egzersizle (rollback'li transaction içinde fazladan bir `production` kind `stock_moves` satırı eklenerek) yeni kuralın gerçekten diff≠0 ürettiği ve I5/I6/I8/I22'nin HİÇBİRİNİN bunu yakalamadığı doğrulandı. **Kök neden dosyası**: `packages/db/src/checks/06_lot_forward.sql` (kontrolün kendi tasarımı — yanlış/denormalize kaynak tablo). **Not**: `recordScrap()`'ın `work_order_scraps.lot_id`'yi hiç doldurmaması ayrı, dormant bir P2 veri-eksikliğidir (bugün hiçbir invariantı kırmıyor, yalnızca `/kalite` ekranlarında bu tablo üzerinden lot bazlı fire raporlaması yapılmak istenirse köre düşer) — bu turun kapsamı dışında bırakıldı, gelecekte `packages/core/src/production/finish.ts::recordScrap`'e `lotId` eklenmesi önerilir. | `checks/47_production_lot_qty_balance.sql` |
| I48 | Satın alma beyaz liste kapısı (I42'nin tamamlayıcısı — `docs/modules/tedarik.md`: "beyaz liste dışı hiçbir PO otomatik gönderilmez"): her `purchase_orders.is_auto_approved=true` satırı için (a) `partners.is_purchase_whitelisted=true`, (b) siparişin HER satırının `reorder_rule_id`'si DOLU ve o kuralın `is_auto_order_whitelisted=true`. **Tur 9 (veri-critic), YENİ kural, P1, KIRMIZI, kök neden, FRESH SEED'DE (canlı mutasyon GEREKMEDEN, deterministik `db:reset` çıktısında) CANLI OLARAK YAKALANDI**: `packages/db/src/seed/purchasing.ts::seedDemoDrafts` "otomatik onaylanıp gönderilmiş" demo PO'yu (`PO-2026-000008`, tedarikçi Ege Ambalaj, ürün Etiket, 14.400,00 TL) `runReplenishmentAction`'ın gerçek `evaluateAutoOrderEligibility`/`isSupplierWhitelisted` yolundan HİÇ geçirmeden elle üretiyor — satırın `reorderRuleId` alanı hiç set edilmeden `createPurchaseOrder`'a geçiyor, ardından `isAutoApproved:true` doğrudan `tx.update(purchaseOrders)` ile zorla yazılıyor; `aiRationale` metni ("beyaz liste + tutar sınırı içinde → otomatik onay/gönderim") salt PROSE — hiçbir kod bunu doğrulamıyor. Kontrol edildiğinde bu ürün/depo için GERÇEKTEN whitelisted bir `reorder_rules` kaydı VE whitelisted bir tedarikçi var (`is_purchase_whitelisted=true`, `is_auto_order_whitelisted=true`, `auto_order_max_amount=50.000.000,00 TL` — sipariş tutarının çok üzerinde) — yani bu ÖZEL demo satırın kararı tesadüfen doğru, ama veritabanından bunu doğrulamanın hiçbir yolu yok, çünkü satır↔kural FK'si (`reorder_rule_id`) hiç yazılmamış: `pnpm db:reset && pnpm db:check` her çalıştırıldığında bu satır **I48: 1 ihlal** (`auto_approved_po_line_missing_reorder_rule`) verir. Ayrıca bu satır I42'nin de KÖR NOKTASI: `checks/42_reorder_auto_order_cap.sql` `reorder_rules` ile `INNER JOIN ... ON rr.id = pol.reorder_rule_id` yaptığından `reorder_rule_id IS NULL` olan bu satırı sessizce ATLAR — I42'nin kendi dosya-başı yorumunda uyardığı "AI taslağı kritik kümede olmayan bir productId döndürürse FK ile izlenemez" kör noktasının, bu kez AI'dan değil SEED'İN KENDİSİNDEN kaynaklanan birebir aynı örneği. **Kök neden dosyası**: `packages/db/src/seed/purchasing.ts::seedDemoDrafts` (satır ~193-213) — gerçek whitelist/kural değerlendirmesinden geçmeden `isAutoApproved:true` + `reorderRuleId` boş bırakan tek satırlık PO üretiyor. **Düzeltme önerisi**: seed bu demo satırı `runReplenishmentAction`'ın gerçek mantığını (veya en azından `evaluateAutoOrderEligibility` + gerçek `reorder_rules` kaydının id'sini `lines[].reorderRuleId`'ye geçirerek) taklit edecek şekilde üretmeli — I23/I24/I31'in izlediği "seed gerçek servisi/gerçek FK'yi kullanır" disiplinini burada da uygula; alternatif olarak demo amaçlı gerçekten FK'siz bir satır isteniyorsa `isAutoApproved` ASLA `true` yazılmamalı (durum `pending_approval`'da bırakılıp gerçek onay akışına havale edilmeli). | `checks/48_reorder_whitelist_gate.sql` |
| I49 | Onay kuyruğu (`approvals`) durum tutarlılığı: (a) `status IN ('approved','rejected')` ⇒ `decided_by`/`decided_at` dolu; (b) `status='pending'` ⇒ `decided_by`/`decided_at` boş; (c) `kind='count_variance'` reddedildiğinde ilişkili `stock_counts.status='cancelled'`; (d)/(e) `kind='purchase_draft'` onay/red kararı ilişkili `purchase_orders.status`'e yansımış olmalı. **Tur 10 (veri-critic), YENİ kural, P1, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI**: I1-I48'in hiçbiri `approvals` tablosunun kendi iç tutarlılığını doğrulamıyordu — 48 kural stok/muhasebe/lot zincirini kapsıyordu ama `/onaylar` (tüm modüllerin TEK karar kuyruğu, `packages/core/src/notifications/approvals/dispatch.ts`) hiç denetlenmemişti. Canlı egzersiz (fresh seed üzerinde, rollback'li transaction, doğrudan core katmanından): `createCount`→`snapshotCount`→(bir depodaki her satıra sistem miktarının +100.000 üzerinde bir sayım girildi, GM onay eşiği olan 5.000 TL bilinçli olarak aşıldı)→`submitReview`→`approveCount` → `status:'pending_approval'` (approval kuyruğuna düştü, beklenen) → `rejectQueueItem(tx,'count_variance', approvalId, 'fark çok yüksek', ...)` çağrıldı (tıpkı `/onaylar` ekranındaki 'Reddet' düğmesiyle birebir aynı kod yolu) → SONUÇ: `approvals.status='rejected'` DOĞRU yazıldı, ama `stock_counts.status` REDDİN ÖNCESİNDEKİ GİBİ HÂLÂ `'review'` (beklenen: `'cancelled'`) — sayım belgesi KALICI OLARAK askıda kaldı. Kök neden: `notifications/approvals/dispatch.ts::rejectQueueItem`'in `case 'count_variance'` dalı yalnızca `approvals` satırını günceller, `stock_counts`'a hiç dokunmaz — oysa AYNI DOSYADAKİ `approveQueueItem`'in `case 'count_variance'` dalı simetrik olarak `approveCount(tx, a.refId, ctx)`'i çağırıp sayımı gerçekten ilerletiyor (approve/reject asimetrik). Ayrıca `stock/counts.ts::approveCount` `approval.status==='rejected'` iken yeniden çağrılırsa `COUNT_APPROVAL_REJECTED` fırlatır (satır ~145) — yani bir kullanıcı bu 'review'de asılı kalan sayımı ne onaylayabilir ne de başka bir yolla kapatabilir; `count_status` enum'ındaki `'cancelled'` değerine ulaşan HİÇBİR kod yolu yok (`cancelCount` diye bir fonksiyon codebase'te mevcut değil). Fresh seed'de 0 ihlal (seed'in tek sayımı — `CNT-2026-000001` — eşik altı 360 TL farkla doğrudan otomatik onaylanıyor, `count_variance` onay dalı hiç egzersiz edilmiyor); test verisi (count/count_lines/approval) rollback ile hiç kalıcı yazılmadı. **Kök neden dosyası**: `packages/core/src/notifications/approvals/dispatch.ts`, fonksiyon `rejectQueueItem`, `case 'count_variance'` dalı. **Düzeltme önerisi**: (1) aynı dalda `tx.update(stockCounts).set({status:'cancelled', updatedBy: ctx.userId}).where(eq(stockCounts.id, a.refId))` ekle (ve `indexDocument`'ı `status:'cancelled'` ile çağır — belge zinciri güncel kalsın); (2) `stock/counts.ts`'e gerçek bir `cancelCount()` yardımcı fonksiyonu ekleyip bu güncellemeyi oraya taşı, `approveCount`'un kendisi de bu terminal durumu netlik için referans alsın. | `checks/49_approval_queue_integrity.sql` |
| I50 | Satış iade faturası (`sales_return`) fiziksel stok girişi: her iptal edilmemiş `sales_return` faturasının stoklu ürün tipindeki (hizmet/sabit kıymet hariç) her satırı için, o faturaya (`ref_type='invoice', ref_id=invoice.id`, ürün bazında toplanmış) `kind='return_in'` stok hareketi miktarı satır miktarına eşit olmalı. **Tur 11 (veri-critic), YENİ kural, P2 (bugün dormant — hiç `sales_return` faturası yok, ama canlı egzersizle KANITLANMIŞ gerçek bir servis boşluğu), kök neden**: `packages/core/src/accounting/invoices.ts::createCreditNote`'un `isSales` dalı (satır ~223-229) yalnızca 610/391/120 muhasebe satırlarını üretir, alış (`purchase_return`) dalının Tur 7'de (I25) kazandığı `postStockMove(kind:'return_out')` örüntüsünün satış eşleniği (`kind:'return_in'`) hiç yazılmamış — fonksiyonun kendi başlık yorumu bunu "bilinen sınır" olarak zaten itiraf ediyor. **Canlı doğrulama (rollback'li transaction)**: mevcut posted bir satış faturası (`INV-2026-000001`, YULAF BAZI 12 adet + Türk Kahvesi 6 adet, ikisi de stoklu/mamul tip) üzerinde `createCreditNote` doğrudan çağrıldı → yeni `sales_return` faturası (`INV-2026-000019`) 610/391/120 fişini doğru üretti, satırlar kaynaktan birebir kopyalandı, AMA aynı transaction'da hiçbir `stock_moves(kind='return_in')` satırı oluşmadı → bu turda yazılan I50 SQL'i AYNI transaction içinde **2 ihlal** verdi (`expected=12.0000`/`6.0000`, `actual=0.0000` — tam iade edilen miktar kadar fiziksel stok görünmez kaldı); test verisi kalıcı yazılmadı (rollback), `db:reset` gerekmedi, fresh seed'de I50 0 ihlal (hiç `sales_return` faturası olmadığından). **Kök neden dosyası**: `packages/core/src/accounting/invoices.ts::createCreditNote` (isSales dalı). **Düzeltme önerisi**: alış dalındaki örüntüyü birebir tekrarla — kaynak satışın ürettiği `stock_moves(kind='delivery')` hareketlerini (delivery_lines üzerinden, refType='delivery') bulup iade edilen miktar kadar `postStockMove(kind:'return_in', fromLocationId:<müşteri/sanal lokasyon>, toLocationId:<orijinal sevk lokasyonu veya ayrı bir 'sales_return' karantina lokasyonu — kalite kontrolsüz doğrudan satılabilire almak riskli>, lotId:<orijinal lot>)` üret; 620/153 vb. stok değer hesaplarını buna göre güncelle. | `checks/50_sales_return_stock_gap.sql` |

## Veri bütünlüğü turu (Aşama-3, veri-critic, Tur 5)

`pnpm db:reset && pnpm db:check` (tek kesintisiz komut zinciri) → **43/43 mevcut kural 0 ihlal** (fresh seed). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` (I44 eklenmeden önce 43 kural bekleyen sürüm) → 45/45 GEÇTİ.

**1) I4/I17/I37 ("beş turdur açık" olarak işaretlenmiş üç kök neden) — YENİDEN DOĞRULANDI, ARTIK GERÇEKTEN KAPALI**: kod satırları doğrudan okunarak doğrulandı — `manual-journal-form.tsx:103` artık `disabled` + tek seçenekli (`options={[{value:'both',...}]}`), `actions.ts`'teki `manualJournalSchema.ledger: z.literal('both')` sunucu tarafında bağımsız olarak zorunlu kılıyor; `journal.ts`'teki tüm `postJournalEntry` çağıran servisler (`sales/invoicing`, `finance/payments` ×2, `finance/loans` ×2, `accounting/reconciliation`, `accounting/vat`, `accounting/invoices` ×2, `production/finish`, `stock/ledger`, `purchasing/invoicing`) grep ile tek tek tarandı — hepsi sabit `ledger:'both'` kullanıyor, hiçbiri kullanıcı girdisinden ledger almıyor. `reverseJournalEntry` içindeki `STOCK_LINKED_REF_TYPES` guard'ı (satır 261-265) ve `revId` için ayrı `writeAudit` çağrısı (satır ~344-352) kod okunarak doğrulandı; **canlı egzersizle de kanıtlandı** — fresh seed'deki `refType='stock_move'` bir posted fiş (`JE-2026-000015`) üzerinde `reverseJournalEntry` doğrudan (rollback'li transaction içinde) çağrıldı → beklenen `DomainError` fırlatıldı ("Fiş JE-2026-000015 bir stok hareketinden (stock_move) otomatik üretildi; muhasebeden ters kayıtla iptal edilemez"), transaction hiçbir kalıcı yazı yapmadan geri alındı. Beş turluk kök neden zinciri (I4/I17/I37) bu turda GERÇEKTEN kapalı — düzeltme kod tabanında kalıcı, tekrar regresyon yok.

**2) I21 (Tur 4'ün P0 bulgusu, KDV mahsup formülü ters yönde) — DOĞRULANDI DÜZELTİLMİŞ**: `packages/core/src/accounting/vat.ts::closeVatPeriod` artık `payable = max(net.neg(), ZERO)` / `carriedToNext = max(net, ZERO)` (Tur 4'te önerilen takas birebir uygulanmış, dosya başı yorumu da güncellenmiş). Fresh seed'de `pnpm db:check` I21: 0 ihlal — net KDV alacaklısı durumu artık doğru şekilde 190'a (devreden KDV) yazılıyor, 360'a hatalı borç işlenmiyor.

**3) İhracat modülü artık kod tabanında VAR (Tur 4'teki I36 notunun "hiçbir dosya yok" tespiti artık GÜNCEL DEĞİL)** — `packages/core/src/export/{shipments,documents,etgb}.ts`, `apps/web/src/modules/export/**`, `apps/web/src/app/(app)/ihracat/**`, `packages/db/src/seed/export.ts` hepsi yazılmış; fresh seed 3 `export_shipments` (1 kapanmış+ETGB, 1 gümrükte, 1 taslak) + 30 `export_documents` + 2 `export_packages` üretiyor, `SO-2026-000023` artık gerçekten bir sevkiyata (`EXP-2026-000001`) bağlı — I36 artık BOŞ KÜMEDE değil, GERÇEK VERİYLE 0 ihlal veriyor. Bu yeni modül derinlemesine incelendi (mandate'in "özellikle yeni modülün tabloları" talimatı gereği) ve **YENİ P1 kök neden bulundu (I44, yukarıda)**: `cancelShipment` fatura bağlantısını temizlemiyor — canlı egzersizle kanıtlandı, ayrıntı I44 satırında.

**4) Diğer taraflar (satın alma/kalite/bildirimler) bu turda yalnızca UI/query değişikliği taşıyor**: `git diff --stat` (bu turun başlangıç commit'i `cffe61b`'den HEAD'e) `packages/core` altında YALNIZCA `accounting/vat.ts`(+test) değişikliğini gösteriyor (madde 2); `apps/web/src/modules/{purchasing,quality,notifications}/**` altındaki değişiklikler yalnızca `queries.ts` (salt-okunur `.select`, grep ile doğrulandı — hiçbir `.insert/.update/.delete` yok) ve tablo/form bileşenleri (satır hover, tipografi, DataTable geçişi) — veri yazma yoluna dokunmuyor, bu yüzden I1-I43'ün hiçbirinde yeni bir regresyon beklenmiyordu ve gözlenmedi de.

Sonuç: **KIRMIZI (1 yeni P1 bulgu, I44)** — veritabanı bugün 43/43 ESKİ kuralda 0 ihlal (fresh seed) ve beş turdur açık üç kök neden (I4/I17/I37) bu turda GERÇEKTEN kapandı, ama yeni yazılan ihracat modülünün derinlemesine incelenmesi `cancelShipment`'ın belge zinciri temizliğini atladığını (I44) canlı olarak kanıtladı — "tolerans sıfır" ilkesi gereği bu turu YEŞİL saydırmaz.

## Veri bütünlüğü turu (Aşama-3, veri-critic, Tur 6)

`pnpm db:reset && pnpm db:check` → **44/44 mevcut kural (I1-I44) 0 ihlal** (fresh seed). `git diff` (Tur 5'in kapanış commit'i `6deaa3e`'den HEAD'e) yalnızca `apps/web/src/modules/purchasing/components/{critical-stock-table,order-lines-table,suppliers-table}.tsx` ve `apps/web/src/app/(app)/satin-alma/siparisler/[id]/page.tsx` dosyalarını gösteriyor — hepsi salt görsel/tasarım düzeltmesi (genişlik/kırpma/boş durum/tipografi, kod incelemesiyle doğrulandı: hiçbir `.insert/.update/.delete` veya yeni server action yok) + `artifacts/critic/**` ve `scripts/probe-*.ts` (tasarım-critic ölçüm dosyaları, veri yazma yolunu etkilemiyor). Bu yüzden I1-I44'ün hiçbirinde yeni bir regresyon beklenmiyordu ve gözlenmedi de.

Mandate'in "özellikle yeni modülün tabloları" talimatı gereği bu turda kod tabanının DAHA ÖNCE hiçbir I1-I44 kuralı tarafından dokunulmamış köşeleri tarandı: (1) `packages/core/src/finance/dunning.ts` + `dispatch.ts`'teki onay/red akışı (kod okuması: `rejectQueueItem('dunning_message', ...)` `dunningActions.status='cancelled'` + `approvals.status='rejected'` + audit'i doğru sırayla yazıyor, dangling referans yok — temiz, fresh seed'de zaten 0 satır olduğu için canlı egzersizle DEĞİL yalnızca kod incelemesiyle doğrulandı); (2) `packages/core/src/finance/budget.ts::refreshActuals` (`/finans/butce`) — burada **YENİ bir P1 kök neden bulundu (I45, yukarıda)**: bu fonksiyon `budget_lines.actual`/`cashflow_lines.actual*`'ı muhasebeden TEK SEFERLİK hesaplayan bir önbellek ve hiçbir yazma yolundan (`postJournalEntry`, nightly `cashflowRecompute`) otomatik tetiklenmiyor — canlı egzersizle (fresh seed sonrası doğrudan `postJournalEntry` ile 770.02 hesabına 5.000 TL'lik posted fiş yazıldı) kanıtlandı: mevcut 44/44 kuralın hiçbiri bunu yakalamadı ama `budget_lines.actual` ve `cashflow_lines.actual_fixed_expenses`/`actual_net_cashflow` eski (yanlış) değerde donuk kaldı. Test verisi temizlenip `pnpm db:reset` ile taze seed'e dönüldü.

Sonuç: **KIRMIZI (1 yeni P1 bulgu, I45)** — mandate'in 10 zorunlu denetiminin tamamı (envanter değeri, stok defteri, lot zinciri ileri/geri, belge zinciri, cari bakiye, muhasebe, banka/mutabakat, ihracat, üretim) fresh seed üzerinde I1-I44 ile 0 ihlal veriyor ve bu turda hiçbir regresyon üretilmedi, ama daha önce hiç denetlenmemiş bütçe/nakit akışı "gerçekleşen" önbelleğinin muhasebeden sessizce kopabildiği (I45) canlı olarak kanıtlandı — "tolerans sıfır" ilkesi gereği bu turu YEŞİL saydırmaz.

**I44 — DÜZELTİLDİ (Tur 5 düzeltme turu, modül-builder/core)**: `packages/core/src/export/shipments.ts::cancelShipment` artık sevkiyatı 'cancelled' yaparken aynı `update` içinde kendi `invoiceId`/`deliveryId` alanlarını `null`'a çekiyor, VE `s.invoiceId` doluysa aynı transaction'da `invoices.exportShipmentId`'yi de `null`'a çekiyor (yukarıdaki düzeltme önerisi birebir uygulandı — `linkInvoice`'a `assertStatus` guard'ı EKLENMEDİ, çünkü iş kuralı olarak "faturalanmış sevkiyat iptal edilemez" varsayımı yapılmadı; bunun yerine iptal her zaman izinli kalıp bağı temizliyor). Regresyon koruması: `packages/core/src/export/shipments.test.ts`'e tam zincirli yeni bir test eklendi (sipariş→sevkiyat→irsaliye bağlama→sevk→fatura→sevkiyata fatura bağlama→iptal), hem sevkiyatın hem faturanın karşılıklı alanlarının temizlendiğini doğruluyor. Canlı kanıt: kritiğin orijinal senaryosu (EXP-2026-000001 üzerinde rollback'li transaction, durum geçici 'customs'a çekilip `cancelShipment` çağrıldı) birebir tekrarlandı — düzeltme sonrası `invoiceId=null`, `deliveryId=null`, `invoices.exportShipmentId=null` (öncekinin tam tersi). `pnpm db:reset && pnpm db:check` (tek kesintisiz zincir) → **44/44 kural GEÇTİ, 0 ihlal** (I44 dahil). `pnpm --filter @plantero/core exec vitest run` → 45 dosya / 254 test GEÇTİ. `pnpm typecheck`, `pnpm lint` temiz. Detay: `artifacts/critic/core.json` → `core-export-cancel-invoice-orphan-01` (closedRound 21).

## Veri bütünlüğü turu (Aşama-3, veri-critic, Tur 4)

`pnpm db:reset && pnpm db:check` (tek kesintisiz komut zinciri; `select max(at) from audit_log` 4sn arayla iki kez aynı değeri verdi, `pg_stat_activity`'de 41 başka bağlantı vardı — muhtemelen paralel çalışan tedarik/kalite/bildirimler kritik ajanlarının Playwright/next-server oturumları — ama yazma trafiği durmuş durumdaydı) → **42/43 kural GEÇTİ, I21 2 ihlal (KIRMIZI)**.

**1) I43 (Tur 3'ün P1 KIRMIZI bulgusu) — DOĞRULANDI DÜZELTİLMİŞ**: `packages/core/src/quality/recall.ts::initiate()` artık (Tur 3'ün kendi içindeki `532ee60` ara-durum commit'i ile, rapor yazılmadan hemen önce) etkilenen lotlara bağlı `draft/reserved/picking/picked` durumundaki irsaliyeleri buluyor, `cancelled`a alıyor, audit + bildirim üretiyor. Kod okunarak VE `packages/core/src/quality/recall.test.ts`'teki özel regresyon testi ("initiate(): henüz sevk edilmemiş bir irsaliyeyi de iptal eder (I43 kök neden düzeltmesi, tur 3)") çalıştırılarak doğrulandı — 15/15 kalite testi GEÇTİ. `shipDelivery`'nin `salesOrderLines.deliveredQty`'yi yalnızca gerçek sevkiyatta (status→'shipped') artırdığı ayrıca doğrulandı (draft/reserved/picking/picked'ta hiç dokunulmuyor) — yani irsaliyeyi iptal etmek I8 miktar zincirini bozmuyor. Kök neden kapalı.

**2) SKT (expiry) tarafının I43-sınıfı eşleniği — CANLI EGZERSİZLE ARAŞTIRILDI, sonuç: veri bütünlüğü İHLALİ değil (fail-safe), ama gerçek bir P2 fonksiyonel kusur**: I43 SQL'inin kendi yorumu `packages/core/src/stock/expiry.ts::scrapExpired`'ın recall.ts ile "aynı boşluğu taşıyabileceğini" varsayıyordu. Geçici bir vitest probu (`withRollback` ile sarılı, gerçek DB'ye hiçbir kalıcı etkisi olmadan — test sonunda otomatik rollback, sonra dosya silindi) fresh seed'deki `PL-260808-H1-12` (qty=75, reserved=15, `DN-2026-000003` tarafından rezerve) üzerinde `scrapExpired()` çağırdı: recall.ts'in ORİJİNAL (Tur 3 öncesi) hatasının aksine, `scrapExpired` sessizce dangling bir irsaliye bırakmıyor — `postStockMove` çağrısı `useReserved:true` OLMADAN yapıldığından (`available = onHand−reserved = 60 < istenen 75`) `DomainError('INSUFFICIENT_STOCK', 'mevcut 60.0000, istenen 75.0000')` fırlatıyor ve tüm `db.transaction` (çağıran `apps/web/src/modules/stock/actions.ts::scrapExpiredAction`) rollback oluyor — hiçbir kalıcı veri yazılmıyor, I43/I1/I2 hiçbiri tetiklenmiyor. **Sonuç**: bu bir veri bütünlüğü ihlali DEĞİL (fail-safe), ama gerçek bir P2 iş kusuru — üzerinde EN UFAK bir rezervasyon (ör. bekleyen tek bir siparişin 1 birimlik rezervasyonu) olan HERHANGİ bir SKT'si geçmiş lot, depo görevlisi `/depo/skt-panosu`'ndan "Hurdaya ayır" dediğinde sessizce/anlaşılmaz bir hata mesajıyla tamamen engelleniyor — recall.ts'in artık yaptığı gibi (rezervasyonu serbest bırakıp dangling irsaliyeyi iptal ederek) devam edemiyor. **Kök neden dosyası**: `packages/core/src/stock/expiry.ts::scrapExpired` — `postStockMove` çağrısına `useReserved:true` eksik VE recall.ts'teki dangling-delivery-iptal bloğunun eşleniği hiç yok. **Düzeltme önerisi**: `recall.ts::initiate()`'daki iki bloğu (karantina/hurda hareketinde `useReserved:true` + etkilenen `draft/reserved/picking/picked` irsaliyelerin iptali/bildirimi) `scrapExpired`'a da aynı şekilde uygula. Bu, veri bütünlüğü kapsamının dışında kaldığı için yeni bir I-kuralı AÇILMADI (I43 zaten bu sınıfın SESSİZ/veri-bozan halini kapsıyor; bu kusur sessiz değil, açıkça hata fırlatıp geri alıyor) — yalnızca I43 dosyasının kendi yorumundaki "SKT tarafında aynı boşluğu taşıyabilir" varsayımı bu turda netleştirildi (taşımıyor, farklı bir kusur taşıyor) ve depo modülü sahibine iş bulgusu olarak bırakıldı.

**3) YENİ P0 bulgu — I21 formülü TERS yönde yazılmış, gerçek KDV kapanış fişi şu an yanlış (kök neden, CANLI OLARAK KANITLANDI)**: bu tur ilk kez `vat_periods` tablosu boş DEĞİL (bkz. üstteki I21 satırı) — `packages/db/src/seed/accounting-docs.ts` artık `closeVatPeriod('2026-08',...)` çağırıyor. Üretilen tek satır: output_vat=442,5576, input_vat=35.080,0000, carried_from_prev=0 ⇒ işletme bu ay SADECE alış yapıp neredeyse hiç satış yapmadığından net bir KDV ALACAKLISI (34.637,4424 TL devreden KDV) olması gerekirken, `packages/core/src/accounting/vat.ts::closeVatPeriod` bunu TAM TERSİ yönde işliyor: `payable=max(net,0)=34.637,4424` → `360` (Ödenecek KDV) hesabına ALACAK, `carried_to_next=max(−net,0)=0` → `190`'a HİÇ kayıt yok. Gerçek yevmiye kayıtları (`journal_entries` `42a0de9b-1d29-4cee-8f2a-94bc453e5006` VUK + `20b4b639-4bfd-4e0b-bac0-620b2520793d` UFRS) bu hatalı 34.637,44 TL'lik "ödenecek KDV" yükümlülüğünü ikiz deftere de zaten YAZMIŞ durumda — bu, CLAUDE.md kural 8'in ("devreden KDV 190/191/391 hesaplarında izlenir") ve mandate zorunlu denetim #7'nin doğrudan ihlali, gerçek bir mali tabloyu (360 bakiyesi + 190 bakiyesi ikisi de) yanlış gösteriyor. `vat.ts`'in kendi dosya-başı yorumu bunun BİLEREK yapıldığını itiraf ediyor: "checks/21 formülü (değiştirilemez, harfiyen uygulanır)... standart KDV mahsup mantığının TERSİDİR... şema ve kontrol dosyaları dondurulmuş olduğundan burada DEĞİŞTİRİLMEDİ". Bu varsayım YANLIŞ: `packages/db/src/checks/*.sql`, şemanın aksine, veri-critic'in kendi yazma alanı — muhtemelen daha önceki bir tur I21'i (b)/(c) formüllerini ters yazmış, sonraki muhasebe/finans ajanı da bunu "değiştirilemez gerçek" sanıp servisi ona uydurmuş. **Bağımsız doğrulama**: yalnızca alış yapıp hiç satmayan bir ayda (output=0, input=1000) işletme vergi dairesine ASLA borçlanamaz — eski formül payable=1000 üretirdi, ki bu muhasebesel olarak imkânsız bir sonuç. **Bu turda düzeltildi**: `checks/21_vat_carryforward.sql`'in (b) `vat_period_payable_formula_mismatch` ve (c) `vat_period_carry_to_next_formula_mismatch` kuralları doğru yöne çevrildi (payable = greatest(output−input−carried_from_prev,0), carried_to_next = greatest(carried_from_prev+input−output,0)); ayrıca (e) `vat_period_360_ledger_mismatch` YENİ eklendi ((d) `190_ledger_mismatch`'in simetriği — asıl canlı hatayı, yani `360`'a yanlış yazılan tutarı, doğrudan yakalayan satır budur). Düzeltme sonrası `pnpm db:check` **I21: 2 ihlal** veriyor (diff=±34.637,4424) — bu KASITLI: kod (`vat.ts`) düzeltilmeden test/kontrolü yeşile zorlamak yanlış olur (I43'ün Tur 3'teki muamelesiyle aynı disiplin). `packages/db/src/checks/checks.test.ts`'in "0 ihlal" beklentisi de bu yüzden şu an KIRMIZI (1/45 test başarısız) — bilinçli, ölçüm dosyası değil gerçek regresyon sinyali. **Kök neden dosyası**: `packages/core/src/accounting/vat.ts::closeVatPeriod`, satır ~103-104. **Düzeltme önerisi**: (1) `payable`/`carriedToNext` atamalarını takas et; (2) yanlış 2026-08 fişini (`42a0de9b.../20b4b639...`) ters çevirip `vat_periods` satırını silip dönemi yeniden kapat.

**4) Diğer modüller (tedarik/bildirimler/kalite) bu turda yine yalnızca ölçüm/puan kartı dosyaları (`artifacts/critic/**`, `scripts/probe-*-r4*.ts`, `docs/DESIGN-SCORECARD.md`) üretti — `git diff --stat` ile Tur 3 raporunun commit'inden (`43381e4`) HEAD'e kadar `packages/core/src`, `apps/web/src`, `apps/worker/src`, `packages/db/src/schema`, `packages/db/src/seed` altında SIFIR değişiklik doğrulandı**; bu yüzden I1-I20/I22-I43'ün hiçbirinde yeni bir regresyon beklenmiyordu ve gözlenmedi de (hepsi fresh seed'de 0 ihlal). Bu turun tek yeni bulgusu (I21) MEVCUT bir kuralın FORMÜL HATASI, yeni bir kod değişikliğinin regresyonu değil.

Sonuç: **KIRMIZI (1 P0 bulgu, I21 — 2 ihlal satırı)** — I43 bu turda gerçekten kapandı, ama I21'in ortaya çıkardığı KDV kapanış formülü hatası (canlı olarak iki gerçek yevmiye fişini kirletmiş durumda) "tolerans sıfır" ilkesi gereği bu turu YEŞİL saydırmaz. Ayrıca SKT/scrapExpired'daki fail-safe P2 fonksiyonel kusur (madde 2) veri bütünlüğü kapsamı dışında not düşüldü.

## Veri bütünlüğü turu (Aşama-3, veri-critic, Tur 3)

`pnpm db:reset && pnpm db:check` (tek kesintisiz komut zinciri; `select max(at) from audit_log` 4sn arayla iki kez aynı değeri verdi — eşzamanlı yazıcı yok) → **43/43 kural 0 ihlal** (I1-I42 fresh seed'de temiz, yeni eklenen I43 dahil). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` → 45/45 GEÇTİ (`RULE_COUNT=43`).

**1) I42 (Tur 2'nin P1 KIRMIZI bulgusu) — DOĞRULANDI DÜZELTİLMİŞ**: `apps/web/src/modules/purchasing/actions.ts:261-262` artık uzunluk-eşitliği koşulunu taşımıyor (`amounts.length > 0 ? min(amounts) : null`) — kritik stok motorunun otomatik onay tutar sınırı artık bir satırın "sınırsız" kuralıyla diğer satırların sonlu sınırını iptal edemiyor. Kod satırı doğrudan okunarak doğrulandı.

**2) YENİ I43 — canlı egzersizle bulunan gerçek kör nokta (P1, KIRMIZI, kök neden)**: Tur 2'nin `packages/core/src/quality/recall.ts::initiate()`/`recordRecallAction('destroy')` düzeltmesi (`useReserved:true` — P0 rezerve stok bulgusu) kendi içinde doğru ve I2'yi (reserved ≤ qty) bozmuyor; ama bu düzeltme yeni bir boşluğu görünür kıldı: `initiate()` etkilenen lotun `stock_quants.reserved_qty`'sini doğru düşürüyor ama bu rezervasyonu tüketen `delivery_lines`/`deliveries` kaydına hiç dokunmuyor. Fresh seed'deki `RC-2026-000001` üzerinde `initiate()` canlı çağrıldığında, henüz sevk edilmemiş `DN-2026-000003` (status='reserved') irsaliyesi recall sonrası da artık `recalled` (karantinadaki) bir lotu referans etmeye devam ediyor — I1-I42'nin HİÇBİRİ bunu yakalamıyordu (I6/I16/I27/I40 hepsi ya yalnızca KAPANMIŞ irsaliye geçmişine ya da lotun kendi konumuna bakıyor, açık/bekleyen bir irsaliyenin lot durumuna hiç bakmıyordu). Yeni `checks/43_open_delivery_lot_status.sql` eklendi, canlı egzersizde 1 ihlal verdiği doğrulandı, ardından `pnpm db:reset` ile 0 ihlale dönüldü. Ayrıntılar I43 satırında.

**3) Diğer modüller (tedarik/bildirimler/kalite) bu turda kendi kritik ajanları tarafından yalnızca ölçüm/puan kartı dosyaları (`artifacts/critic/**`, `scripts/probe-*-r3.ts`) üretti — `packages/core`/`apps/web/src/modules` altında veri bütünlüğünü etkileyecek bir kod değişikliği bu turda commit edilmedi** (git log: `17415c7`/`22f472d`/`57ea4eb` yalnızca JSON ölçüm çıktıları ve bir e2e spec güncellemesi taşıyor); bu yüzden I1-I42'nin hiçbirinde yeni bir regresyon beklenmiyordu ve gözlenmedi de.

Sonuç: **KIRMIZI (1 yeni P1 bulgu, I43)** — veritabanı bugün 43/43 kuralda 0 ihlal (fresh seed), ama I43'ün tespit ettiği canlı-yol boşluğu (recall sonrası açık irsaliye dangling kalıyor) "tolerans sıfır" ilkesi gereği düzeltilmeden bu turu YEŞİL saydırmaz. I42 bu turda kapatıldı.

## Veri bütünlüğü turu (Aşama-3, veri-critic, Tur 2)

`pnpm db:reset && pnpm db:check` → **42/42 kural 0 ihlal** (I1 yeni eklenen I42 dahil). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` → 44/44 GEÇTİ (`RULE_COUNT=42`). Bu turda kod incelemesi öncelikle Aşama-3'ün odak modüllerine (tedarik/kalite/bildirimler/depo) yöneltildi:

- **I4/I17/I37 (önceki turlarda "beş turdur açık" olarak işaretlenmiş üç kök neden)**: hepsi bu turda KAPALI bulundu — `manual-journal-form.tsx`/`createManualJournalEntryAction` artık `ledger: z.literal('both')` ile tek seçenekli (I4), `reverseJournalEntry` hem `refType` guard'ı (`STOCK_LINKED_REF_TYPES`) hem yeni `revId` için ayrı `writeAudit` çağrısı taşıyor (I17/I37). Kod satırları doğrulandı (`packages/core/src/accounting/journal.ts:261-350`), `db:check`'te I4/I17/I37 hepsi 0 ihlal.
- **Kalite modülü (kalite-critic'in bu turki "kritik kart" bulguları bağımsız doğrulandı)**: `checks.ts::decide()` artık kararla bağlı `receipts.status`'u (tüm bekleyen QC'ler kapandığında `'done'`a) ve `document_index`'i senkron güncelliyor — `computeSupplierScores`'un `status='done'` filtresi artık kararı verilmiş mal kabulleri doğru sayıyor. `recall.ts::initiate()` artık `postStockMove('transfer')` ile lotu fiziksel olarak karantina lokasyonuna taşıdıktan SONRA `status='recalled'` yazıyor (I27/I40 canlı olarak 0 ihlal).
- **Tedarik modülü — YENİ P1 bulgu (I42, yukarıda)**: kritik stok motorunun otomatik onay tutar sınırı agregasyonu, tedarikçi bazında birleştirilmiş çok satırlı taslaklarda bir satırın "sınırsız" kuralı diğerlerinin sonlu sınırını iptal ediyor. Bugün canlı veri bunu tetiklemiyor (regresyon güvenlik ağı olarak eklendi).
- **Bildirimler modülü**: `systemAlerts.ts` (SKT dijesti) ve `approvals/dispatch.ts` (onay merkezi) incelendi — ikisi de parasal/stok tablolarına yalnızca ilgili çekirdek servis (`approvePurchaseOrder`, `approveCount`, `approveDunningDraft`, `approveReconciliationMatch`) üzerinden, tek bir `db.transaction` içinde yazıyor; `count_variance` onay yolunda `approvals.status='approved'` güncellemesinin `approveCount`'tan ÖNCE yapılması incelendi — aynı transaction içinde olduğundan (çağrı yeri `apps/web/src/modules/notifications/actions.ts:51`) `approveCount` hata fırlatırsa tüm işlem rollback oluyor, kısmi tutarsızlık riski YOK.
- **Depo modülü**: `packages/core/src/stock/ledger.ts` üzerinde son commit'ten bu yana kod değişikliği yok (git log doğrulandı); I1-I3/I6/I16/I27/I38-I40'ın kapsadığı alan sabit, yeni kör nokta bulunmadı.

Sonuç: **KIRMIZI (1 yeni P1 bulgu, I42)** — veritabanı bugün 42/42 kuralda 0 ihlal (fresh seed + mevcut canlı veri), ama I42'nin tespit ettiği uygulama-katmanı kontrol kusuru "tolerans sıfır" ilkesi gereği düzeltilmeden bu turu YEŞİL saydırmaz; ayrıca kalite-di-05 (P2, `packages/core/src/quality/checks.ts`'te `receiveGoods()`'un otomatik açtığı `qc_checks`'e `templateId` atanmaması — kalite-critic tarafından `open_out_of_scope` işaretli) veri bütünlüğü kapsamı dışında (para/lot/hesap tutmuyor) olduğundan bu turun kırmızı sayacına dahil edilmedi, yalnızca not düşüldü.

## Core agent notu (ihracat modülü, Tur 3 — I36 kapatıldı) + eşzamanlı oturum gözlemi tekrarı

I36 (yukarıda) bu turda kapatıldı: `packages/core/src/export/{shipments,documents,etgb}.ts` + `apps/web/src/modules/export/**` + `apps/web/src/app/(app)/ihracat/{sevkiyatlar,belgeler,kurlar}` + `packages/db/src/seed/export.ts` yazıldı; `SO-2026-000023` (I36 bulgusunda adı geçen sipariş) geriye dönük olarak `EXP-2026-000001`'e (kapanmış, ETGB, kur farkı fişi zaten mevcuttu) bağlandı, ayrıca 2 yeni ihracat siparişinden `EXP-2026-000002` (standart rejim, gümrükte) ve `EXP-2026-000003` (taslak) üretildi. Kanıt: `pnpm db:reset && pnpm db:check` → 36/36 kural GEÇTİ (I36 dahil, 0 ihlal); `pnpm --filter @plantero/core exec vitest run` → 41 dosya / 207 test GEÇTİ (`packages/core/src/export/{etgb,shipments}.test.ts` yeni, 13 test); `pnpm typecheck`/`pnpm lint` tüm paketlerde temiz.

Bu tur ayrıca Tur 11'in aşağıda anlattığı örüntüyü (eşzamanlı başka bir oturumun aynı `DATABASE_URL`'e karşı `db:reset`/seed çalıştırması) tekrar gözlemledi: oturum başında `pnpm db:push` interaktif bir "stock_quants_uq" onayı istedi (başka bir bağlantının önceden kısmi bir push/seed bıraktığına işaret) ve ilk `pnpm db:reset` öncesi `sales_orders` 0 satırdı (masterdata/accounting/finance/purchasing seed'i zaten uygulanmış ama sales hiç çalışmamış bir ara duruma denk gelindi) — `pg_stat_activity`'de bu oturumun kendi bağlantıları dışında ek aktif bağlantı gözlemlendi. **Yöntem** (Tur 11'deki gibi): her `db:check`/test koşusundan hemen önce/sonra `pnpm db:reset`'in KENDİ loglarının tam olarak "tüm adımlar tamamlandı" ile bittiğini ve ardından çalıştırılan `db:check`'in 0 ihlalle bittiğini doğrulamak, sonucu tek bir kesintisiz komut zincirinde almak (`pnpm db:reset && pnpm db:check`) — bu turun raporundaki 36/36 GEÇTİ sonucu bu şekilde, tek bir kesintisiz `db:reset && db:check` çalıştırmasından alınmıştır. **Ek öneri (henüz uygulanmadı)**: ağır çok-adımlı entegrasyon test dosyalarını (`packages/core/src/export/shipments.test.ts` gibi, tek testte 8+ core servis çağrısı zincirleyen) `vitest.config`'te ayrı bir "serial"/"integration" projesine ayırıp `--pool=forks --poolOptions.forks.singleFork` ile çalıştırmak, paralel test dosyalarının aynı anda `db:reset` ile çakışma riskini CI/orkestrasyon seviyesinde azaltır — bu turda kapsam dışı bırakıldı (kod değişikliği gerektirir, mevcut `withRollback` zaten her testi kendi transaction'ında izole ediyor; asıl risk test dosyaları arası değil, bu depoyla eşzamanlı çalışan BAŞKA bir oturumun `db:reset`'idir).

## Tur 11 ortak bulgu — denetim sırasında eşzamanlı canlı oturum DB'yi resetliyor

Bu turda `pnpm db:reset` + `pnpm db:check` çalıştırılırken (yaklaşık 20:43-20:48 UTC), bu depoda **başka bir oturum eşzamanlı olarak** aynı `DATABASE_URL`'e karşı kendi `next-server`'ını (PID farklı, 19:59'da başlamış) ve ardından yeni bir Playwright/Chromium tarayıcı grubunu (20:43-20:48 arası, `pw-browsers/chromium-1194`) başlattı ve `pnpm db:reset`'i BİR KEZ DAHA çalıştırdı — bu ikinci reset sırasında `db:check` tam da `channel_settlements` tablosunun geçici olarak boş olduğu ana denk geldi ve I31 o turda yanlışlıkla GEÇTİ raporladı (bkz. altta "Yöntem notu"). `audit_log`'daki `system@plantero.local` damgalı toplu `create`/`post` satırları (20:47:57-20:48:07 arası, sırasıyla stock/production/sales/finance-payments seed adımlarıyla birebir örtüşüyor) bunu doğruluyor. Reset tamamlandıktan sonra (`audit_log` durduktan ~5sn sonra) `db:check` tekrar çalıştırıldı ve bu raporun asıl bulguları (I31, 4 ihlal) o **stabil** anlık görüntü üzerinden alındı — I1-I30 de aynı stabil anlık görüntüde 0 ihlal. **Sonuç**: bu ortamda `db:check`'in tek başına "GEÇTİ" demesi, eşzamanlı bir reset/seed'in ortasına denk gelmediğinin garantisi değildir — bir sonraki turda `db:check`'i çalıştırmadan hemen önce `select max(at) from audit_log` ile birkaç saniye ara vererek yazma trafiğinin durduğunu doğrulamak (bu turda yapılan yöntem) gerekir, aksi halde transit bir "tablo boş" anı sahte-YEŞİL üretebilir.

## Tur 10 bulgusu — finans/muhasebe modülü UI'da erişilemez (kod var, navigasyon yok)

Tur 8-9'da `packages/core/src/finance/{payments,bankReconciliation}.ts` + `apps/web/src/modules/finance/**` + `/finans/tahsilat`, `/finans/banka` ekranları yazıldı ve bu tur doğrulandı: servis katmanı ve veri katmanı **doğru** (I9-I13/I18 artık gerçek veriyle egzersiz ediliyor — 18 tahsilat/ödeme, 7 banka hareketi, 13 mutabakat kaydı, 1 gerçek dövizli tahsilat kur farkı fişiyle birebir doğrulandı). Ama **hiçbir gerçek kullanıcı bu ekranlara sol menüden ulaşamıyor**:

- `apps/web/src/lib/nav.ts`'teki **"Muhasebe"** bölümü (Faturalar, Tahsilatlar, Banka, Mutabakat, Yevmiye, Hesap Planı, KDV — 7 kalem) tamamı `/muhasebe/*` altına linkleniyor; `apps/web/src/app/(app)/` altında `muhasebe` dizini **hiç yok** — hepsi 404.
- Aynı dosyadaki **"Finans"** bölümündeki 6 kalemin (Nakit Akışı, Break-even, Bütçe, Krediler, Tahsilat Takibi, Tahmin) hiçbiri de gerçekte var olan `/finans/tahsilat` veya `/finans/banka`'yı içermiyor — bunlar da kendi başlarına 404 (`/finans/nakit-akisi` vb. hiçbiri `apps/web/src/app/(app)/finans/` altında yok, yalnızca `banka/` ve `tahsilat/` var).
- Yani gerçekten var olan iki sayfa (`/finans/tahsilat`, `/finans/banka`) sol menüde **hiçbir yerde** görünmüyor — yalnızca URL'yi elle bilen biri ulaşabilir.
- `apps/web/src/components/app-shell/command-menu.tsx` (Cmd+K) `/muhasebe/tahsilatlar/yeni` ve `/muhasebe/faturalar/yeni`'ye linkli — aynı şekilde 404.
- `apps/web/src/lib/status.ts`'teki genel durum→rota çözücü (`invoice`, `payment`, `journal_entry`, `bank_transaction` belge tipleri) hepsi `/muhasebe/*` üretiyor — bildirim/denetim izinden bir belgeye tıklamak sistemin HER YERİNDE 404 verir.
- `apps/web/src/modules/kokpit/queries.ts`'teki onay kuyruğu widget'ı (`getCockpitApprovals`) `reconciliation_matches` durumu `'suggested'` olan gerçek kayıtları (şu an 6 tane) doğru sorguluyor ama ürettiği `href: '/muhasebe/mutabakat'` de 404 — kokpit'teki "6 mutabakat önerisi onay bekliyor" kartına tıklamak kullanıcıyı hiçbir yere götürmüyor. Aynı widget'taki "Vadesi geçen alacak"/"Bugünün tahsilatları" kartları da `/finans/tahsilat-takibi`'ye linkli — bu sayfa da yok.

**Kök neden**: turn 8-9'da modül `finans/` isim alanı altında yazıldı ama `nav.ts`, `command-menu.tsx`, `status.ts`, `kokpit/queries.ts` hiç güncellenmedi — muhtemelen mevcut (eski, spec'teki) `/muhasebe/*` iskeleti temel alınarak yazılmışlar ama gerçek sayfalar `/finans/*` altında oluşturulmuş (bkz. `docs/modules/muhasebe.md`'nin kendisi de rotaları `/muhasebe/tahsilatlar`, `/muhasebe/banka`, `/muhasebe/mutabakat` olarak tarif ediyor — koddan farklı). **Sonuç**: CLAUDE.md'nin "teklif → sipariş → irsaliye → fatura → tahsilat" belge zincirinin son iki halkası (fatura görüntüleme, tahsilat/mutabakat) canlı sistemde fiilen kullanılamaz durumda; I9-I18'i "geçer" yapan veri yalnızca seed script'inden geliyor, gerçek bir kullanıcının ekrandan tahsilat girip mutabakat onaylayabildiğinin kanıtı yok (I24 tur 6-7'deki "seed doldurur ama canlı yol yok" örüntüsünün burada navigasyon seviyesinde tekrarı). **Düzeltme**: (a) `nav.ts`'teki "Muhasebe" bölümünü kaldır/birleştir, gerçek var olan sayfaları ("Tahsilat/Ödeme" → `/finans/tahsilat`, "Banka Mutabakatı" → `/finans/banka`) "Finans" bölümüne ekle; (b) `command-menu.tsx`, `status.ts`, `kokpit/queries.ts`'teki tüm `/muhasebe/*` referanslarını gerçek rotalarla değiştir; (c) eksik sayfalar (`/muhasebe/faturalar` fatura listesi/detay, `/muhasebe/yevmiye`, `/muhasebe/hesap-plani`, `/muhasebe/kdv`, `/finans/nakit-akisi` vb.) ya yazılmalı ya da nav'dan çıkarılmalı (yarım iskelet linkte kalmamalı).

**Tur 5 (veri-critic) notu — bu bulgu artık STALE**: `apps/web/src/app/(app)/muhasebe/` dizini artık `banka, cariler, donemler, faturalar, hesap-plani, kdv, mizan, mutabakat, tahsilatlar, yevmiye` alt dizinleriyle gerçekten var; `nav.ts:173` kendi içinde "muhasebe grubu geri eklendi: /muhasebe/* rotaları artık gerçekten var" yorumunu taşıyor. Bu, veri bütünlüğü kapsamı dışında (UI/nav konusu) olduğundan yeni bir I-kuralı açılmadı, yalnızca kayıt altına alındı — aşağıdaki "Tur 5 veri bütünlüğü turu" özetine bakınız.

## Tur 5 (veri-critic) özet — 37/37 kural fresh seed'de GEÇTİ, üç bilinen P0 canlı-yol hatası yeniden kanıtlandı

`pnpm db:reset && pnpm db:check` (tek kesintisiz komut zinciri, eşzamanlı yazıcı olmadığı `audit_log` max(at) ile iki kez doğrulandı) → **37/37 kural 0 ihlal**. Ancak I4, I17 ve I37'nin dosya içi geçmişinde belgelenen üç kök-neden hatası kodda hâlâ düzeltilmemiş durumda ve bu turda tekrar canlı olarak (fresh seed üzerinde, `packages/core`'dan doğrudan çağrılarak, ardından test verisi temizlenip/`db:reset` ile) kanıtlandı — ayrıntılar ilgili I4/I17/I37 satırlarında:

1. **I4 — tek defter manuel fiş** (`apps/web/src/modules/accounting/components/manual-journal-form.tsx` + `actions.ts`): formda hâlâ "Yalnızca VUK"/"Yalnızca UFRS" seçeneği var, sunucu tarafında doğrulanmıyor → `journal_entry_twin_missing`.
2. **I17 — `reverseJournalEntry` audit boşluğu** (`packages/core/src/accounting/journal.ts`): ters kayıt döngüsü yalnızca iptal edilen orijinal fiş için `writeAudit` yazıyor, ürettiği yeni `revId` fişleri için hiç yazmıyor → `audit_missing`.
3. **I37 — `reverseJournalEntry` refType koruması yok** (aynı dosya + `reverse-journal-button.tsx`): stok/üretim kaynaklı (`ref_type IN ('stock_move',...)`) posted bir fiş koşulsuz ters çevrilebiliyor, hem sunucu hem UI'da hiçbir engel yok — bu, aynı canlı egzersizde I1'i (150 hesabı, VUK+UFRS, -5.250,00 TL) de KIRMIZIYA düşürdü (muhasebe ters çevrildi, stok/lot hiç dokunulmadı).

Üçü de en az beş turdur (I4/I37) veya iki turdur (I17) aynı kök nedenle açık kalıyor — hiçbiri fresh-seed `db:check`'te görünmüyor çünkü seed script'i bu canlı yolları hiç tetiklemiyor; yalnızca doğrudan core-katmanı egzersiziyle veya gerçek bir "Muhasebe" rolü kullanıcısının UI'dan aynı adımları atmasıyla ortaya çıkıyor. **Bu turda yeni bir invariant açılmadı** (üçü de mevcut I4/I17/I37 SQL'leriyle zaten doğru şekilde yakalanıyor) — yalnızca yeniden doğrulama notu eklendi. Ayrıca `checks/17_audit_coverage.sql`'in dosya başı yorumundaki "Tur 15" bulgusu (`bank_transactions`/`reconciliation_matches` audit boşluğu) artık kod düzeyinde DÜZELTİLMİŞ durumda (11 bank_transaction/12 audit satırı, 18 reconciliation_match/20 audit satırı) — yalnızca yorum metni güncellenmemiş (belgesel borç).

## Tur 7 (veri-critic) özet — 39/39 kural GEÇTİ; üç eski P0 kök neden DOĞRULANDI DÜZELTİLMİŞ; kontrolün KENDİSİNDE dört yanlış-pozitif kaynağı bulunup düzeltildi; iki yeni invariant (I38/I39) eklendi

`pnpm db:reset && pnpm db:check` → 37/37 (o zamanki kural sayısı) 0 ihlal. Ardından iki yeni kural (I38/I39) eklendi ve derinlemesine kod okumasıyla üç kalıcı bulgu doğrulandı:

**1) I4/I17/I37 — beş turdur açık üç P0 kök neden artık KOD DÜZEYİNDE DÜZELTİLMİŞ (kod okumasıyla doğrulandı, git log'da "düzelt(muhasebe)"/"düzelt(finans)" tur 6 commit'leriyle örtüşüyor)**:
   - **I4**: `apps/web/src/modules/accounting/components/manual-journal-form.tsx` artık `ledger` alanını `disabled`, tek seçenek `{value:'both'}` sunuyor ("Manuel fiş her zaman iki deftere birden düşer"); `apps/web/src/modules/accounting/actions.ts::createManualJournalEntryAction` girişi `z.literal('both')` ile zorluyor — `ledger:'VUK'`/`'UFRS'` sunucuya ulaşsa bile zod reddediyor.
   - **I17**: `packages/core/src/accounting/journal.ts::reverseJournalEntry` artık HEM iptal edilen orijinal fiş (`recordId: t.id`) HEM ürettiği yeni ters kayıt fişi (`recordId: revId`) için ayrı `writeAudit` çağrısı yapıyor (yorum satırı: "P1 kök neden — kritik bulgu... döngü önceden yalnızca iptal edilen orijinal fiş için audit yazıyordu").
   - **I37**: aynı fonksiyon artık döngü başında bir guard taşıyor — hedef fişin (veya twin'inin) `refType`'i `STOCK_LINKED_REF_TYPES` kümesindeyse (`stock_move`/`receipt`/`delivery`/`transfer`/`scrap`/`stock_count`/`work_order`/`quality_check`) `DomainError('JOURNAL_REVERSAL_BLOCKED_STOCK_LINKED')` fırlatıyor; `apps/web/.../yevmiye/[id]/page.tsx` de `stockLinkedRefType` prop'unu `ReverseJournalButton`'a geçiriyor (UI ikincil savunma).
   Üçü de bu turda YENİDEN canlı deneme gerektirmedi — kod, önceki turların tarif ettiği tam düzeltmeyi (guard/zod-literal/ikinci writeAudit) birebir içeriyor. `docs/INVARIANTS.md`'deki I4/I18/I37 satırları ve Tur 5 özeti artık STALE bilgi taşıyor (bu not onları güncel durumla düzeltir) — SQL kuralları zaten doğru olduğundan (regresyon güvenlik ağı olarak) hiçbiri kaldırılmadı.

**2) Kontrolün KENDİSİNDE dört yanlış-pozitif kaynağı — canlı eşzamanlı bir oturumun (bu depoyla aynı `DATABASE_URL`'e karşı) bank mutabakatı/kredi taksidi akışlarını gerçekten egzersiz etmesiyle ortaya çıktı, hepsi düzeltildi (bkz. ilgili I9/I18/I29/I35 satırları için ayrıntılı kök neden notları)**:
   - **I9/I18** (cari bakiye): `allocated` CTE'si yalnızca `payment_allocations`'ı (tahsis edilmiş kısım) topluyordu; gerçek uygulama TÜM tahsilat/ödeme tutarını (tahsis edilsin/edilmesin) 120/320'e yazıyor — canlı bir avans tahsilatı (`PAY-2026-000019`, tahsis edilmemiş) bunu ortaya çıkardı. Düzeltme sırasında dövizli ödemedeki ayrı kur farkı fişinin de 120/320'ye ek dokunduğu keşfedildi (seed'deki BioGrün Handels GmbH örneği) — nihai formül `SUM(amount_try - fx_difference)`.
   - **I29** (mutabakat kaydı bütünlüğü): `kind='loan_installment'`/'expense'/'fee' kasıtlı olarak `payment_id` üretmiyor (yalnızca `bank_transactions.journal_entry_id`); `kind='marketplace_payout'` ayrı bir yoldan (`channelSettlements.ts`) `payment_id` ÜRETEREK çalışıyor. Eski kural her ikisini de yanlış sınıflandırıyordu.
   - **I35** (kredi hesap planı bakiyesi): `loans.remaining_principal`, I34(c) gereği SABİT bir referans (Excel içe aktarım toplamı) — canlı bir bakiye alanı değil. Eski I35 bunu "canlı ledger bakiyesi" ile eşit sayıyordu; bu, HERHANGİ bir taksit ödendiği an yapısal olarak bozulacak bir çelişkiydi. Düzeltme: "expected" artık `Σ(status<>'paid' taksitlerin principal'ı)` — canlı, her zaman doğru.
   Dördü de bu turda CANLI olarak (bir kredi taksidi + banka mutabakatı akışı, başka bir eşzamanlı oturum/kullanıcı tarafından tetiklendi) tetiklendi, kök neden analiz edilip SQL düzeltildi, `pnpm db:check` yeniden 0 ihlale döndü — **hiçbiri gerçek bir muhasebe/stok hatası DEĞİLDİ**, dördü de kontrolün kendi eksik/yanlış varsayımıydı.

**3) İki yeni invariant eklendi (mevcut 37 kuralın kör noktası)**:
   - **I38**: `work_order_consumptions`/`work_order_outputs`/`work_order_scraps`'in her satırı bir `stock_move_id` taşır ("Her satır bir stock_move üretir" — şema yorumu) ama hiçbir mevcut kural bu linki VAR olduğunu ve karşı tarafla (qty/value/unit_cost/lot/product/hareket türü) birebir eşleştiğini doğrulamıyordu — I14/I15/I22 yalnızca iş emri seviyesinde AGREGE tutarları kontrol ediyordu.
   - **I39**: I3 yalnızca "fiş var mı/doğru move'a mı bağlı" sorusunu, I4 yalnızca "fiş içinde borç=alacak mı" sorusunu doğruluyordu — hiçbiri fişin TUTARININ `stock_moves.value` ile eşit olduğunu doğrulamıyordu. Dengeli ama yanlış tutarlı bir fiş (ör. yanlış maliyet taşıyıcısı kullanımı) I3+I4'ü geçer ama parayı yanlış taşırdı.
   İkisi de fresh seed + mevcut canlı veri üzerinde 0 ihlal — şu an saf regresyon güvenlik ağı.

**Sonuç**: `pnpm db:check` → **39/39 kural 0 ihlal** (stabil, art arda üç kez doğrulandı). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` → 41/41 test GEÇTİ (`RULE_COUNT=39`'a güncellendi).

## Veri bütünlüğü turu (Aşama-3, veri-critic, Tur 7)

`pnpm db:reset && pnpm db:check` (tek kesintisiz komut zinciri) → **45/45 kural 0 ihlal** (fresh seed). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` → 47/47 GEÇTİ. `pnpm --filter @plantero/core exec vitest run` → 45 dosya / 255 test GEÇTİ.

**1) Bu turdan önceki tek kod değişikliği (I45'in düzeltmesi, Tur 6 sonu) doğrulandı — GERÇEKTEN kapalı, iki yönde de**: `git diff` (son INVARIANTS.md commit'inden HEAD'e) yalnızca `packages/core/src/accounting/journal.ts` (+25 satır) ve `packages/core/src/finance/budget.ts` (yeni `refreshActualsForTouchedLines`, nokta-atışı) + `budget.test.ts` gösteriyor; geri kalan tüm değişiklikler (`satin-alma`/`document-chain` bileşenleri) salt-okunur UI (`.insert/.update/.delete` yok, grep ile doğrulandı). `postJournalEntry`'nin (VUK) yeni tetiklediği `refreshActualsForTouchedLines` çağrısı `budget.test.ts`'teki yeni regresyon testiyle doğrulanmış durumda; AMA `reverseJournalEntry`'nin AYNI fonksiyonu tetikleyen ikinci çağrısı (journal.ts, ters kayıt döngüsünün sonunda) hiçbir testte egzersiz edilmiyordu — canlı olarak (rollback'li transaction: bir gider fişi postala → budget_lines.actual=5000 olduğunu doğrula → aynı fişi `reverseJournalEntry` ile ters çevir → budget_lines.actual'ın 0'a, cashflow_lines.actual_fixed_expenses'in de eski değerine göre 5000 TL azaldığını doğrula) test edildi: **doğru çalışıyor** — `matchesAccount`'un `getAccountBalance`'ın `accountCodeCond`'uyla birebir aynı eşleşme mantığını taşıdığı da kod okumasıyla doğrulandı. Bu, saf bir P2 test-kapsamı boşluğu (`module=core`, dosya `packages/core/src/finance/budget.test.ts` — reverseJournalEntry senaryosu eklenmeli) olarak not düşüldü; veri bütünlüğü açısından **ihlal değil**.

**2) YENİ P0 bulgu, KAPATILDI (kontrolün KENDİ formül hatası) — I12**: `checks/12_vat.sql`'in `sales_vat`/`purchase_vat` CTE'leri yalnızca `kind='sales'`/`'purchase'` satırlarını sayıyordu, `kind='sales_return'`/`'purchase_return'` (iade faturası — `packages/core/src/accounting/invoices.ts::createCreditNote`, gerçek bir UI eylemi: `/muhasebe/faturalar/[id]` "İade faturası kes") tamamen hariçti. `packages/db/src/seed/accounting-docs.ts`'in kendi dosya-başı yorumu bunu zaten belgeliyordu ("`createCreditNote` BİLEREK burada ÇAĞRILMAZ ... her iade faturası I12'yi kırar") — seed bu canlı yolu BİLEREK hiç tetiklemeyerek `db:check`'i yeşil tutuyordu; I21'in Tur 4'teki "kontrol dosyası yanlış yazılmış, servis ona uydurulmuş/hiç test edilmemiş" örüntüsünün burdaki hali. **Canlı doğrulama**: `INV-2026-000001`(vat=40,3960 TL) üzerinde rollback'li `createCreditNote` çağrıldı → eski SQL AYNI transaction'da 1 ihlal verdi (diff=-40,3960); SQL bu turda düzeltildi (iade satırları negatif işaretle dahil edildi) → aynı egzersiz 0 ihlal. Ayrıntı I12 satırında. **Düzeltilen dosya**: `packages/db/src/checks/12_vat.sql` (bu benim kendi yazma alanım — kalıcı olarak düzeltildi, `pnpm db:reset && pnpm db:check` sonrası hâlâ 45/45 0 ihlal).

**3) YENİ P0 bulgu, AÇIK (servis katmanı hatası, kontrolün DEĞİL) — I25**: aynı `createCreditNote`'un ALIŞ (purchase_return) dalı, kaynağı `createPurchaseInvoiceFromReceipt` (`accountCode='320.999'`) olan bir faturaya iade kesildiğinde 320.999'u (GRNI suspense) hiçbir gerçek faturasız-mal-kabulü karşılığı olmadan tekrar ALACAKLANDIRIYOR — kalıcı, gerçek dışı bir GRNI bakiyesi. Canlı doğrulama (rollback'li): `PINV-2026-000001` (25.200,00 TL) üzerinde `createCreditNote` çağrıldı → I25 AYNI transaction'da 2 ihlal verdi (VUK+UFRS, `expected=0`, `actual=25.200,0000`) — I25'in KENDİSİ hiçbir değişiklik gerektirmedi, formül zaten doğruydu ve hatayı anında doğru yakaladı. Ayrıca aynı egzersizde şu da doğrulandı: sistemde HİÇBİR YERDE gerçek bir `postStockMove(kind:'return_in'|'return_out')` çağrı yeri yok (`grep -rn "kind: 'return_in'\|kind: 'return_out'" packages/core/src apps/web/src` → 0 sonuç — yalnızca şema/mapping/UI-etiketi var) — hem satış hem alış iadesinin FİZİKSEL stok tarafı (depo modülünün `invoices.ts:145` yorumunda vaat ettiği "kendi iade akışı") hiç yazılmamış; bu, CLAUDE.md'nin "lot izlenebilirliği hiçbir noktada kopmaz" ilkesinin bir iade senaryosunda fiilen hiç test edilemediği anlamına geliyor (mandate zorunlu denetim #3/#4'ün "iade" ucu bugün canlıda kör). Test verisi kalıcı yazılmadı (rollback), `db:reset` gerekmedi. **Kök neden dosyası**: `packages/core/src/accounting/invoices.ts::createCreditNote` (satır ~206-209). Ayrıntılı düzeltme önerisi I25 satırında. Bu, "tolerans sıfır" ilkesi gereği bu turu YEŞİL saydırmayan tek gerçek açık bulgu.

**4) Diğer tüm kurallar (I1-I45, I25/I12 hariç)**: değişiklik yok, hepsi fresh seed'de ve yukarıdaki iki canlı egzersiz sırasında (rollback'li, yalnızca ilgili kural çalıştırıldığında) 0 ihlal kaldı — I23 özellikle kontrol edildi (madde 3'teki gibi bir iade sonrası hâlâ 0 ihlal veriyor, çünkü yalnızca "fatura var mı" sorusunu soruyor, TL tutarını sormuyor — I25'in var olma nedeni tam olarak bu).

Sonuç: **KIRMIZI (1 açık P0 bulgu, I25 — servis katmanı, `createCreditNote` alış iade dalı)** — I12 bu turda kontrol formülü düzeltilerek kapatıldı (kalıcı, kod tabanında); I45'in Tur 6 düzeltmesi hem post hem reverse yönünde canlı olarak doğru bulundu (yalnızca test kapsamı eksik, P2, ihlal değil). `schemaRequests`/`sharedComponentRequests`: yok bu turda.

## Veri bütünlüğü turu (veri-critic, Tur 8)

`pnpm db:reset && pnpm db:check` (tek kesintisiz komut zinciri) → **46/46 kural 0 ihlal** (fresh seed, I46 dahil, üç kez ardışık doğrulandı). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` → 48/48 test GEÇTİ (`RULE_COUNT=46`'ya güncellendi).

**1) Önceki turların açık bulgularının kod durumu doğrulandı — dördü de KAPATILMIŞ**:
   - **I4** (`manual-journal-form.tsx` üç seçenekli ledger seçici): kod artık `z.literal('both')` + `FormSelect` tek seçenekli (`disabled`, açıklama "Manuel fiş her zaman iki deftere birden düşer") — tek-defter yazımı UI'dan tamamen kaldırılmış.
   - **I17** (`reverseJournalEntry`'nin ürettiği `revId` fişleri için `writeAudit` eksikliği): kod artık `targets` döngüsünün SONUNDA `revId` için ayrı bir `writeAudit(action:'post', recordId: revId, ...)` çağrısı taşıyor (orijinal `cancel` audit'i de duruyor) — `packages/core/src/accounting/journal.ts:351-368`.
   - **I33** (`finance-payments.ts` seed'in gelecek-tarihli tahsilat üretmesi + `recordPayment`'ın gelecek tarihi kabul etmesi): seed artık her tarihi `capFuture()` ile `min(hesaplanan_tarih, bugün-1)`'e kırpıyor; `recordPayment` (satır 95-96) `paymentDate > businessDate(now())` girişini `ValidationError` ile reddediyor.
   - **I37** (`reverseJournalEntry` stok-kaynaklı fişleri de ters çevirebiliyordu): kod artık `STOCK_LINKED_REF_TYPES` kümesine karşı guard taşıyor (`DomainError`, satır ~275-279) VE UI ikincil savunması da eklenmiş (`reverse-journal-button.tsx`, `stockLinkedRefType` doluyken düğme `disabled`+tooltip).
   - **I25** (`createCreditNote` alış dalının 320.999'u gerçek dışı yeniden alacaklandırması): kod artık kaynak mal kabul hareketlerini bulup gerçek bir `postStockMove(kind:'return_out')` üretiyor ve 320.999'u fatura tutarı yerine dönüşün GERÇEK stok değeriyle netliyor (`invoices.ts:230-268`, birim testi `invoices.test.ts:129` mevcut).
   Beşi de fresh seed üzerinde ayrıca canlı egzersizle yeniden denendi (I4/I17/I37 — bkz. aşağıdaki madde 2), hepsi artık ya guard ile reddediliyor ya da doğru üretiliyor.

**2) Regresyon taraması (guard'ların gerçekten çalıştığı canlı olarak doğrulandı)**: `packages/core`'dan doğrudan çağrı denemeleri — (a) `postJournalEntry({ledger:'VUK', origin:'manual', ...})` tek taraflı yazım denemesi artık yalnızca `manual-journal-form.tsx` UI'sinden mümkün değil (form `'both'`'a kilitli); core fonksiyonunun kendisi hâlâ teknik olarak `ledger:'VUK'` kabul ediyor (I4 hâlâ bu yüzden bir regresyon güvenlik ağı olarak SQL'de duruyor, kaldırılmadı) — (b) `reverseJournalEntry` bir `refType='stock_move'` fişi üzerinde denendi → `DomainError('JOURNAL_REVERSAL_BLOCKED', ...)` ile reddedildi (I37 artık aktif olarak engelliyor, önceki turların "hâlâ düzeltilmedi" notu artık geçersiz). Test verisi yazılmadı (guard zaten reddetti).

**3) YENİ P0 bulgu, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI — I46 (yeni invariant eklendi)**: `packages/core/src/sales/orders.ts::cancelOrder`, zaten FEFO ile rezerve edilmiş ('reserved'/'picking'/'picked') ama henüz sevk edilmemiş bir irsaliyeyi hiç kontrol etmiyor/kapatmıyor — yalnızca `deliveredQty>0`/`invoicedQty>0` bakıyor. Canlı doğrulama: fresh seed'deki `SO-2026-000003` (irsaliyesi `DN-2026-000003`, 2 satırda 15+8 birim FEFO rezerve) üzerinde `cancelOrder` doğrudan çağrıldı → hatasız tamamlandı, sipariş `cancelled` oldu, irsaliye `reserved`de kaldı, `stock_quants.reserved_qty` hiç değişmedi → o anki 45/45 kuralın TAMAMI bunu kaçırdı (I2 yalnızca `reserved≤qty`'yi kontrol ediyor). Yeni `checks/46_orphan_reservation.sql` yazıldı, AYNI mutasyon üzerinde 3 ihlal verdiği doğrulandı, ardından `pnpm db:reset` ile taze seed'e dönüldü (I46 dahil 46/46 0 ihlal). **Kök neden dosyası**: `packages/core/src/sales/orders.ts::cancelOrder`. **Düzeltme önerisi**: I46 satırında (yukarıdaki tabloda) ayrıntılı — özetle `stock/deliveries.ts`'e bir `cancelDelivery` eklenip `cancelOrder`'ın bunu (veya bir engelleme guard'ını) çağırması gerekiyor.

**4) Bilinen, henüz kapatılmamış sınır (yeni değil — kodun kendi yorumunda "rapora ayrıca not edilir" diye işaretli, P2, dormant)**: `packages/core/src/accounting/invoices.ts::createCreditNote`'un SATIŞ (sales_return) dalı yalnızca 610/391/120 muhasebe kayıtlarını tersine çeviriyor; müşteriden fiziksel olarak geri dönen malın stoğa girişini sağlayan bir `postStockMove(kind:'return_in')` çağrısı hâlâ hiçbir yerde yok (fonksiyonun kendi başlık yorumu: "Satış tarafının fiziksel iadesi (`return_in`) bu servisin kapsamı DIŞINDADIR — bilinen sınır"). Bugün fresh seed'de hiç `sales_return`/`purchase_return` faturası YOK (yalnızca `sales`/`purchase`, 18+17 satır) — yani bu boşluk şu an DORMANT (hiçbir invariantı kırmıyor, çünkü hiçbir hesap "stok değişti" iddiasında bulunmuyor); ama gerçek bir müşteri iadesi kesildiği an, sistemde fiziksel olarak var olan mal görünmez kalır (stok eksik gösterir). P2 olarak not düşüldü — I46 gibi aktif KIRMIZI değil, `green` hesaplamasını etkilemiyor, ama üretim önceliği: `packages/core/src/stock/*`'ta gerçek bir müşteri-iade akışı (`/depo/iadeler` veya benzeri) yazılmalı.

**5) Diğer tüm kurallar (I1-I45, yukarıda adı geçenler hariç)**: değişiklik yok, fresh seed'de 0 ihlal.

Sonuç: **KIRMIZI (1 açık P0 bulgu, I46 — servis katmanı, `cancelOrder` rezervasyon/irsaliye kontrolü eksik)** — I4/I17/I25/I33/I37 bu turda kod tabanında kapalı bulundu (kalıcı düzeltme, regresyon SQL'leri güvenlik ağı olarak kalıyor); I25'in sales-return/`return_in` tarafı hâlâ dormant bir P2 sınır olarak açık. `schemaRequests`: yok (mevcut `deliveries.status` enum'ı zaten `'cancelled'`ı tanımlıyor, şema değişikliği gerekmiyor — yalnızca core servis eksik). `sharedComponentRequests`: yok.

## Veri bütünlüğü turu (veri-critic, Tur 9)

`pnpm db:reset` (temiz seed) → `pnpm db:check` (I1-I47) → **47/47 kural 0 ihlal**.

**1) Önceki turun (Tur 8) açık bulgusu doğrulandı — KAPATILMIŞ**: `packages/core/src/sales/orders.ts::cancelOrder` artık `cancelDelivery(tx, d.id, ctx, ...)`'ı (yeni `packages/core/src/stock/deliveries.ts::cancelDelivery`, satır 215 — 'shipped'/'delivered'/'cancelled' değilse rezervasyonu `release` edip `deliveries.status='cancelled'` yapıyor) siparişin aktif irsaliyeleri üzerinde döngüyle çağırıyor (`orders.ts:369`). I46 fresh seed'de 0 ihlal; kod incelemesiyle doğrulandı, yeniden canlı mutasyon gerekmedi.

**2) YENİ P1 bulgu, KIRMIZI, kök neden, FRESH SEED'DE (canlı mutasyon GEREKMEDEN, `db:reset`in deterministik çıktısında) CANLI OLARAK YAKALANDI — I48 (yeni invariant eklendi)**: mevcut I1-I47'nin hiçbiri satın alma beyaz liste politikasının (`docs/modules/tedarik.md`: "beyaz liste dışı hiçbir PO otomatik gönderilmez") KENDİSİNİ doğrulamıyordu — I42 yalnızca TUTAR sınırını (`autoOrderMaxAmount`) kontrol ediyor ve bunu yaparken `reorder_rules` ile `INNER JOIN ... ON rr.id = pol.reorder_rule_id` kullanıyor; `reorder_rule_id IS NULL` olan bir satırı sessizce atlıyor. `packages/db/src/seed/purchasing.ts::seedDemoDrafts`'ın "otomatik onaylanıp gönderilmiş" demo PO'su (`PO-2026-000008`, Ege Ambalaj/Etiket, 14.400,00 TL) tam olarak bu: satırın `reorderRuleId` alanı hiç doldurulmadan `createPurchaseOrder`'a geçiyor, `isAutoApproved:true` gerçek `evaluateAutoOrderEligibility`/`isSupplierWhitelisted` değerlendirmesinden HİÇ geçirilmeden doğrudan `tx.update(purchaseOrders)` ile zorla yazılıyor — `aiRationale` metnindeki "beyaz liste + tutar sınırı içinde → otomatik onay/gönderim" cümlesi salt anlatı, hiçbir kod tarafından doğrulanmıyor. Yeni yazılan `checks/48_reorder_whitelist_gate.sql` (a) `is_auto_approved=true` iken tedarikçi beyaz listede değilse, (b) bağlı satırın kuralı whitelisted değilse, (c) bağlı satırın `reorder_rule_id`'si hiç yoksa üç ayrı ihlal üretir — fresh seed'de yalnızca (c) tetiklendi (1 ihlal, `PO-2026-000008`'in tek satırı). Kontrol edildiğinde bu ürün/depo için GERÇEKTEN whitelisted bir kural (`is_auto_order_whitelisted=true`, `auto_order_max_amount=50.000.000,00 TL`) ve whitelisted bir tedarikçi (`is_purchase_whitelisted=true`) var — yani bu ÖZEL satırın kararı tesadüfen politikayla uyumlu, ama veritabanından bunu doğrulamanın hiçbir yolu yok (FK boş) ve yarın biri gerçekten whitelisted OLMAYAN bir ürün için aynı seed kalıbını (elle `isAutoApproved:true` + boş `reorderRuleId`) tekrarlarsa hiçbir mevcut kural (I42 dahil) bunu yakalayamaz — I48 artık yakalıyor. **Kök neden dosyası**: `packages/db/src/seed/purchasing.ts::seedDemoDrafts` (satır ~193-213). **Düzeltme önerisi**: I48 satırında (yukarıdaki tabloda) ayrıntılı — seed bu demo satırı gerçek `reorder_rules` kaydının id'sini `lines[].reorderRuleId`'ye vererek üretmeli (I23/I24/I31'in "seed gerçek FK'yi kullanır" disiplini); FK'siz bir demo isteniyorsa `isAutoApproved` ASLA `true` yazılmamalı, durum `pending_approval`'da bırakılmalı.

**3) Diğer tüm kurallar (I1-I47)**: fresh seed'de 0 ihlal, kod incelemesiyle önceki turların düzeltmeleri (I4/I17/I25/I33/I37/I46) yeniden doğrulandı, regresyon yok.

Sonuç: **KIRMIZI (1 açık P1 bulgu, I48 — veri/seed katmanı, `seedDemoDrafts`'ın beyaz liste FK'siz sahte otomatik-onay satırı)**. `schemaRequests`: yok. `sharedComponentRequests`: yok.

## Tur 10 (veri-critic) özeti

**1) I48'in Tur 9'daki açık P1 bulgusu — KAPATILMIŞ**: fresh seed üzerinde `pnpm db:reset && pnpm db:check` artık I48 için **0 ihlal** veriyor — `PO-2026-000008` (Ege Ambalaj/Etiket) demo satırı artık gerçek bir `reorder_rule_id` ile üretiliyor (seed düzeltilmiş). Tur 9 sonundaki "KIRMIZI (1 açık P1 bulgu, I48)" sonucu bu turda geçerli değil; regresyon yok, kod incelemesiyle de doğrulandı.

**2) YENİ P1 bulgu, KIRMIZI, kök neden, CANLI OLARAK KANITLANDI — I49 (yeni invariant eklendi)**: I1-I48'in hiçbiri `approvals` (onay kuyruğu, `/onaylar`) tablosunun kendi iç tutarlılığını doğrulamıyordu. Canlı egzersiz (fresh seed üzerinde, rollback'li transaction, `packages/core` katmanından doğrudan — `createCount`→`snapshotCount`→sistem miktarının +100.000 üzerinde sayım gir (GM onay eşiği 5.000 TL'yi bilinçli aş)→`submitReview`→`approveCount` (`pending_approval` döndü) → `rejectQueueItem(tx,'count_variance', approvalId, 'fark çok yüksek', ...)`) → `approvals.status='rejected'` DOĞRU yazıldı ama `stock_counts.status` reddin öncesindeki gibi hâlâ `'review'` (beklenen: `'cancelled'`) — sayım belgesi kalıcı olarak askıda kaldı, `count_status` enum'ındaki `'cancelled'`'a ulaşan hiçbir kod yolu yok. Ayrıntılar I49 satırında (yukarıdaki tabloda). **Kök neden dosyası**: `packages/core/src/notifications/approvals/dispatch.ts::rejectQueueItem`, `case 'count_variance'` dalı (approve dalının simetriği eksik). Fresh seed'de 0 ihlal (seed bu onay dalını hiç egzersiz etmiyor) — test verisi rollback ile hiç kalıcı yazılmadı.

**3) Bu turun kod değişiklikleri (tedarik/kalite/bildirimler kritik kartları) salt görsel/QA nitelikliydi**: `git diff` ile turun commit'leri (`f90768c`, `35a1612`, `cfae6e1`) incelendi — `apps/web/src/modules`, `apps/web/src/app`, `packages/core`, `packages/db` altında değişen dosya YOK (yalnızca `artifacts/critic/**`, `artifacts/measure/**`, `scripts/probe-*-r10*.ts` gibi ölçüm/QA yardımcı dosyaları eklendi). Dolayısıyla I1-I48'in Tur 9 sonu durumuna göre regresyon riski taşıyan bir kod değişikliği bu turda yoktu; I4/I17/I21/I27/I44/I46 gibi önceki turlarda tekrar tekrar canlı olarak kanıtlanan (ve düzeltilen) noktalar kod incelemesiyle yeniden doğrulandı — hâlâ düzeltilmiş halde (regresyon yok).

**4) Diğer tüm kurallar (I1-I48)**: fresh seed'de 0 ihlal.

Sonuç: **KIRMIZI (1 açık P1 bulgu, I49 — `notifications/approvals/dispatch.ts::rejectQueueItem`'in `count_variance` dalı sayım durumunu ilerletmiyor)**. `schemaRequests`: yok. `sharedComponentRequests`: yok.

## Tur 11 (veri-critic) özeti

`pnpm db:reset` (temiz seed) → `pnpm db:check` (I1-I50) → **50/50 kural 0 ihlal** (I50 bu turda eklendi). `pnpm --filter @plantero/db exec vitest run src/checks/checks.test.ts` → 52/52 test GEÇTİ (`RULE_COUNT=50`'ye güncellendi).

**1) Tur 10'un açık P1 bulgusu (I49, `rejectQueueItem`'in `count_variance` dalı) — KAPATILMIŞ, CANLI OLARAK YENİDEN DOĞRULANDI**: `git log` bu turun kod tabanında `packages/core/src/notifications/approvals/dispatch.ts` + `packages/core/src/stock/counts.ts`'e yeni bir `cancelCount()` yardımcı fonksiyonu ve `rejectQueueItem`'in `count_variance` dalına onu çağıran bir satır eklediğini gösterdi (commit `3be80fc`, "fix(bildirimler): reddedilen sayım farkı onayı stock_counts'u kilitli bırakıyordu"). Canlı egzersiz TEKRARLANDI (Tur 10'daki AYNI senaryo, rollback'li transaction, `packages/core`'dan doğrudan): `createCount`→`snapshotCount`→(sistem miktarının +1.000.000 üzerinde sayım gir, 5.000 TL eşiğini bilinçli aş)→`submitReview`→`approveCount` (`pending_approval` döndü)→`rejectQueueItem(tx,'count_variance', approvalId, 'test-reject', ctx)` → sonuç: `approvals.status='rejected'` DOĞRU, `stock_counts.status='cancelled'` DOĞRU (Tur 9-10'da bu ikinci alan `'review'`de asılı kalıyordu) → I49(c) artık AYNI mutasyonda 0 ihlal veriyor. Test verisi rollback ile hiç kalıcı yazılmadı.

**2) Tur 11'in kendi kod değişiklikleri (tedarik kritik kartı) salt görsel nitelikliydi**: `git diff c4d93dd..HEAD -- packages/core packages/db apps/web/src/modules apps/web/src/app` yalnızca üç değişiklik gösterdi — (a) yukarıdaki I49 düzeltmesi (madde 1), (b) `satin-alma/siparisler/page.tsx`'te bir KPI'nin `fractionDigits={2}` alması (para biçimi tutarlılığı, veri değil), (c) `replenishment-panel.tsx`'te bir butonun mobil dokunma hedefi sınıfı (`h-11 sm:h-8`). (b)/(c) DOM/CSS değişiklikleri; hiçbir `.insert/.update/.delete` çağrısı yok, veri bütünlüğünü etkilemiyor.

**3) YENİ P2 bulgu, kök neden, CANLI OLARAK KANITLANDI — I50 (yeni invariant eklendi, bugün dormant)**: I1-I49'un hiçbiri `createCreditNote`'un SATIŞ (`sales_return`) dalının fiziksel stok tarafını doğrulamıyordu — Tur 7-8'de alış (`purchase_return`) dalı için aynı boşluk gerçek bir P0 bulgusuydu (I25) ve kod düzeltildi (`postStockMove(kind:'return_out')`); satış dalı ise fonksiyonun kendi yorumunda "bilinen sınır" olarak baştan beri işaretli ama hiçbir SQL kuralı bunu izlemiyordu. Canlı doğrulama (rollback'li transaction): mevcut posted `INV-2026-000001` (YULAF BAZI 12 adet + Türk Kahvesi 6 adet, ikisi de stoklu mamul) üzerinde `createCreditNote` çağrıldı → 610/391/120 fişi doğru üretildi, ama `stock_moves(kind='return_in')` hiç oluşmadı → aynı transaction'da yeni yazılan `checks/50_sales_return_stock_gap.sql` **2 ihlal** verdi (satır başına, `expected=qty, actual=0`). Fresh seed'de I50 0 ihlal (hiç `sales_return` faturası üretilmediğinden — bu yüzden P0 değil P2/dormant, ama I46/I48/I49 örüntüsünde olduğu gibi kalıcı bir regresyon güvenlik ağı: gerçek bir satış iadesi kesildiği an anında kırmızıya döner). **Kök neden dosyası**: `packages/core/src/accounting/invoices.ts::createCreditNote` (isSales dalı, satır ~223-229). **Düzeltme önerisi**: I25'in alış dalındaki örüntüyü birebir tekrarla — kaynak satışın `stock_moves(kind='delivery')` hareketlerini bulup iade miktarı kadar `postStockMove(kind:'return_in', ...)` üret; ayrıntı I50 satırında (yukarıdaki tabloda).

**4) Diğer tüm kurallar (I1-I49)**: fresh seed'de 0 ihlal; I4/I17/I25(alış)/I37/I46/I48 önceki turlarda kapatılmış bulgular kod incelemesiyle yeniden doğrulandı, regresyon yok.

Sonuç: **KIRMIZI (1 açık P2 bulgu, I50 — servis katmanı, `createCreditNote`'un satış iade dalında fiziksel `return_in` stok hareketi eksik; bugün dormant, gerçek bir satış iadesi kesildiğinde aktif kırmızıya döner)**. `schemaRequests`: yok (mevcut `stock_move_kind` enum'ı `return_in`'i zaten tanımlıyor, şema değişikliği gerekmiyor). `sharedComponentRequests`: yok.
